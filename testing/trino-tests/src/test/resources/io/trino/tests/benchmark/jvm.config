# JVM args for the launched benchmark JVM. Heap is set via -Xmx/-Xms by BenchmarkRunner.

-server

# Garbage collection / OOM behaviour
-XX:G1HeapRegionSize=32M
-XX:+ExplicitGCInvokesConcurrent
-XX:+ExitOnOutOfMemoryError
-XX:-OmitStackTraceInFastThrow

# JIT
-XX:ReservedCodeCacheSize=512M
-XX:PerMethodRecompilationCutoff=10000
-XX:PerBytecodeRecompilationCutoff=10000

# Misc
-Djdk.attach.allowAttachSelf=true
-Djdk.nio.maxCachedBufferSize=2000000

--add-modules=jdk.incubator.vector

# Lets async-profiler's native unwinder walk past library boundaries
# (libsnappyjava, LZ4, libzstd) into JIT'd Java code.
-XX:+PreserveFramePointer
