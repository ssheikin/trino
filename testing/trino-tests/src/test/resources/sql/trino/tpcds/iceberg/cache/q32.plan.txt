final aggregation over ()
    local exchange (GATHER, SINGLE, [])
        remote exchange (GATHER, SINGLE, [])
            partial aggregation over ()
                cross join:
                    join (LEFT, REPLICATED):
                        join (INNER, REPLICATED):
                            join (INNER, REPLICATED):
                                alternatives
                                    dynamic filter (await [cs_item_sk::EQUAL, cs_sold_date_sk::EQUAL])
                                        scan catalog_sales
                                    cache data
                                        dynamic filter (await [cs_item_sk::EQUAL, cs_sold_date_sk::EQUAL])
                                            scan catalog_sales
                                    load from cache
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPLICATE, BROADCAST, [])
                                        alternatives
                                            scan date_dim
                                            cache data
                                                scan date_dim
                                            load from cache
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPLICATE, BROADCAST, [])
                                    alternatives
                                        scan item
                                        cache data
                                            scan item
                                        load from cache
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPLICATE, BROADCAST, [])
                                final aggregation over (cs_item_sk_20)
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, [cs_item_sk_20])
                                            partial aggregation over (cs_item_sk_20)
                                                join (INNER, REPLICATED):
                                                    alternatives
                                                        dynamic filter (await [cs_sold_date_sk_39::EQUAL])
                                                            scan catalog_sales
                                                        cache data
                                                            dynamic filter (await [cs_sold_date_sk_39::EQUAL])
                                                                scan catalog_sales
                                                        load from cache
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            alternatives
                                                                scan date_dim
                                                                cache data
                                                                    scan date_dim
                                                                load from cache
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPLICATE, BROADCAST, [])
                            single aggregation over ()
                                values (1 rows)
