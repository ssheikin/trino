- OutputNode
  - ProjectNode
    - TopNNode (GPU)
      - ExchangeNode LOCAL (GPU)
        - TopNNode (GPU)
          - JoinNode (GPU)
            - FilterNode (GPU)
              - TableScanNode store_returns (GPU)
            - ExchangeNode LOCAL (GPU)
              - JoinNode (GPU)
                - FilterNode (GPU)
                  - TableScanNode customer_demographics (GPU)
                - ExchangeNode LOCAL (GPU)
                  - JoinNode (GPU)
                    - JoinNode (GPU)
                      - FilterNode (GPU)
                        - TableScanNode household_demographics (GPU)
                      - ExchangeNode LOCAL (GPU)
                        - JoinNode (GPU)
                          - FilterNode (GPU)
                            - TableScanNode customer (GPU)
                          - ExchangeNode LOCAL (GPU)
                            - ProjectNode (GPU)
                              - FilterNode
                                - TableScanNode customer_address (GPU)
                    - ExchangeNode LOCAL (GPU)
                      - ProjectNode (GPU)
                        - FilterNode
                          - TableScanNode income_band (GPU)
