- OutputNode
  - ProjectNode
    - ExchangeNode
      - AggregationNode
        - ExchangeNode (GPU)
          - AggregationNode (GPU)
            - ProjectNode
              - JoinNode
                - FilterNode (GPU)
                  - TableScanNode (GPU)
                - ExchangeNode
                  - JoinNode
                    - ProjectNode (GPU)
                      - FilterNode
                        - TableScanNode (GPU)
                    - ExchangeNode
                      - JoinNode (GPU)
                        - JoinNode (GPU)
                          - JoinNode (GPU)
                            - FilterNode (GPU)
                              - TableScanNode (GPU)
                            - ExchangeNode (GPU)
                              - ProjectNode (GPU)
                                - FilterNode
                                  - TableScanNode (GPU)
                          - ExchangeNode (GPU)
                            - FilterNode (GPU)
                              - TableScanNode (GPU)
                        - ExchangeNode (GPU)
                          - TableScanNode (GPU)
