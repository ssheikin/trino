final aggregation over ()
    local exchange (GATHER, SINGLE, [])
        remote exchange (GATHER, SINGLE, [])
            partial aggregation over ()
                join (INNER, REPLICATED):
                    join (INNER, REPLICATED):
                        dynamic filter ([ws_item_sk::EQUAL, Cast(ws_ext_discount_amt::decimal(7,2), decimal(9,3))::GREATER_THAN], await [ws_sold_date_sk::EQUAL])
                            scan web_sales
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPLICATE, BROADCAST, [])
                                scan date_dim
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPLICATE, BROADCAST, [])
                            join (INNER, PARTITIONED):
                                final aggregation over (ws_item_sk_8)
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, [ws_item_sk_8])
                                            partial aggregation over (ws_item_sk_8)
                                                join (INNER, REPLICATED):
                                                    dynamic filter (await [ws_item_sk_8::EQUAL, ws_sold_date_sk_39::EQUAL])
                                                        scan web_sales
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            scan date_dim
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [i_item_sk])
                                        scan item
