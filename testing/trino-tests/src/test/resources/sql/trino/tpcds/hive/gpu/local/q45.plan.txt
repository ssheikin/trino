- OutputNode
  - TopNNode (GPU)
    - ExchangeNode (GPU)
      - TopNNode (GPU)
        - AggregationNode (GPU)
          - ExchangeNode (GPU)
            - AggregationNode (GPU)
              - ProjectNode
                - FilterNode
                  - ProjectNode (GPU)
                    - SemiJoinNode (GPU)
                      - JoinNode (GPU)
                        - JoinNode (GPU)
                          - JoinNode (GPU)
                            - FilterNode (GPU)
                              - TableScanNode (GPU)
                            - ExchangeNode (GPU)
                              - JoinNode (GPU)
                                - FilterNode (GPU)
                                  - TableScanNode (GPU)
                                - ExchangeNode (GPU)
                                  - ProjectNode (GPU)
                                    - FilterNode
                                      - TableScanNode (GPU)
                          - ExchangeNode (GPU)
                            - TableScanNode (GPU)
                        - ExchangeNode (GPU)
                          - TableScanNode (GPU)
                      - ExchangeNode (GPU)
                        - ProjectNode (GPU)
                          - FilterNode
                            - TableScanNode (GPU)
