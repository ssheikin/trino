- OutputNode
  - TopNNode (GPU)
    - ExchangeNode LOCAL (GPU)
      - TopNNode (GPU)
        - AggregationNode (GPU)
          - ProjectNode (GPU)
            - AggregationNode (GPU)
              - ExchangeNode LOCAL (GPU)
                - AggregationNode (GPU)
                  - JoinNode (GPU)
                    - AssignUniqueId
                      - FilterNode (GPU)
                        - TableScanNode item (GPU)
                    - ExchangeNode LOCAL
                      - ProjectNode
                        - FilterNode
                          - TableScanNode item
