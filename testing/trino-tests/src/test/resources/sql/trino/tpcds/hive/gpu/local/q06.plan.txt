- OutputNode
  - TopNNode (GPU)
    - ExchangeNode (GPU)
      - TopNNode (GPU)
        - FilterNode (GPU)
          - AggregationNode (GPU)
            - ExchangeNode (GPU)
              - AggregationNode (GPU)
                - JoinNode
                  - JoinNode (GPU)
                    - JoinNode (GPU)
                      - FilterNode (GPU)
                        - TableScanNode (GPU)
                      - ExchangeNode (GPU)
                        - JoinNode (GPU)
                          - FilterNode (GPU)
                            - TableScanNode (GPU)
                          - ExchangeNode (GPU)
                            - JoinNode (GPU)
                              - FilterNode (GPU)
                                - TableScanNode (GPU)
                              - ExchangeNode
                                - EnforceSingleRowNode
                                  - ExchangeNode (GPU)
                                    - AggregationNode (GPU)
                                      - ExchangeNode (GPU)
                                        - AggregationNode (GPU)
                                          - ProjectNode (GPU)
                                            - FilterNode
                                              - TableScanNode (GPU)
                    - ExchangeNode (GPU)
                      - TableScanNode (GPU)
                  - ExchangeNode
                    - JoinNode (GPU)
                      - FilterNode (GPU)
                        - TableScanNode (GPU)
                      - AggregationNode
                        - ExchangeNode (GPU)
                          - AggregationNode (GPU)
                            - TableScanNode (GPU)
