local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                join (INNER, REPLICATED):
                    join (INNER, REPLICATED):
                        final aggregation over (cr_returning_customer_sk, ca_state)
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [cr_returning_customer_sk, ca_state])
                                    intermediate aggregation over (cr_returning_customer_sk, ca_state)
                                        join (INNER, PARTITIONED):
                                            remote exchange (REPARTITION, HASH, [cr_returning_addr_sk])
                                                partial aggregation over (cr_returning_customer_sk, cr_returning_addr_sk)
                                                    join (INNER, REPLICATED):
                                                        dynamic filter ([cr_returning_customer_sk::EQUAL, cr_returning_addr_sk::EQUAL], await [cr_returned_date_sk::EQUAL])
                                                            scan catalog_returns
                                                        local exchange (GATHER, SINGLE, [])
                                                            remote exchange (REPLICATE, BROADCAST, [])
                                                                scan date_dim
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPARTITION, HASH, [ca_address_sk])
                                                    dynamic filter ([ca_state::EQUAL])
                                                        scan customer_address
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPLICATE, BROADCAST, [])
                                join (INNER, REPLICATED):
                                    dynamic filter ([c_current_addr_sk::EQUAL])
                                        scan customer
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            scan customer_address
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPLICATE, BROADCAST, [])
                            final aggregation over (ca_state_94)
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [ca_state_94])
                                        partial aggregation over (ca_state_94)
                                            final aggregation over (cr_returning_customer_sk_32, ca_state_94)
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPARTITION, HASH, [cr_returning_customer_sk_32, ca_state_94])
                                                        intermediate aggregation over (cr_returning_customer_sk_32, ca_state_94)
                                                            join (INNER, PARTITIONED):
                                                                remote exchange (REPARTITION, HASH, [cr_returning_addr_sk_35])
                                                                    partial aggregation over (cr_returning_customer_sk_32, cr_returning_addr_sk_35)
                                                                        join (INNER, REPLICATED):
                                                                            dynamic filter ([cr_returning_addr_sk_35::EQUAL], await [cr_returned_date_sk_25::EQUAL])
                                                                                scan catalog_returns
                                                                            local exchange (GATHER, SINGLE, [])
                                                                                remote exchange (REPLICATE, BROADCAST, [])
                                                                                    scan date_dim
                                                                local exchange (GATHER, SINGLE, [])
                                                                    remote exchange (REPARTITION, HASH, [ca_address_sk_86])
                                                                        scan customer_address
