- OutputNode
  - ExchangeNode
    - ExchangeNode
      - SortNode
        - ExchangeNode
          - JoinNode (GPU)
            - ExchangeNode
              - FilterNode (GPU)
                - TableScanNode (GPU)
            - ExchangeNode (GPU)
              - FilterNode (GPU)
                - AggregationNode (GPU)
                  - ExchangeNode
                    - ExchangeNode
                      - AggregationNode (GPU)
                        - JoinNode
                          - ExchangeNode
                            - JoinNode (GPU)
                              - ExchangeNode
                                - JoinNode (GPU)
                                  - ExchangeNode
                                    - FilterNode (GPU)
                                      - TableScanNode (GPU)
                                  - ExchangeNode
                                    - ExchangeNode
                                      - ProjectNode (GPU)
                                        - FilterNode
                                          - TableScanNode (GPU)
                              - ExchangeNode
                                - ExchangeNode
                                  - ProjectNode (GPU)
                                    - FilterNode
                                      - TableScanNode (GPU)
                          - ExchangeNode
                            - ExchangeNode
                              - ProjectNode (GPU)
                                - FilterNode
                                  - TableScanNode (GPU)
