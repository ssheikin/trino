cross join:
    final aggregation over ()
        local exchange (GATHER, SINGLE, [])
            remote exchange (GATHER, SINGLE, [])
                partial aggregation over ()
                    join (INNER, PARTITIONED):
                        remote exchange (REPARTITION, HASH, [ss_customer_sk])
                            join (INNER, REPLICATED):
                                join (INNER, REPLICATED):
                                    join (INNER, REPLICATED):
                                        join (INNER, REPLICATED):
                                            alternatives
                                                dynamic filter ([ss_customer_sk::EQUAL, ss_item_sk::EQUAL], await [ss_promo_sk::EQUAL, ss_sold_date_sk::EQUAL, ss_store_sk::EQUAL])
                                                    scan store_sales
                                                cache data
                                                    dynamic filter ([ss_customer_sk::EQUAL, ss_item_sk::EQUAL], await [ss_promo_sk::EQUAL, ss_sold_date_sk::EQUAL, ss_store_sk::EQUAL])
                                                        scan store_sales
                                                load from cache
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPLICATE, BROADCAST, [])
                                                    alternatives
                                                        scan date_dim
                                                        cache data
                                                            scan date_dim
                                                        load from cache
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPLICATE, BROADCAST, [])
                                                alternatives
                                                    scan store
                                                    cache data
                                                        scan store
                                                    load from cache
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            alternatives
                                                scan item
                                                cache data
                                                    scan item
                                                load from cache
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPLICATE, BROADCAST, [])
                                        alternatives
                                            scan promotion
                                            cache data
                                                scan promotion
                                            load from cache
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPARTITION, HASH, [c_customer_sk])
                                join (INNER, REPLICATED):
                                    alternatives
                                        dynamic filter ([c_current_addr_sk::EQUAL])
                                            scan customer
                                        cache data
                                            dynamic filter ([c_current_addr_sk::EQUAL])
                                                scan customer
                                        load from cache
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            alternatives
                                                scan customer_address
                                                cache data
                                                    scan customer_address
                                                load from cache
    final aggregation over ()
        local exchange (GATHER, SINGLE, [])
            remote exchange (GATHER, SINGLE, [])
                partial aggregation over ()
                    join (INNER, PARTITIONED):
                        remote exchange (REPARTITION, HASH, [ss_customer_sk_20])
                            join (INNER, REPLICATED):
                                join (INNER, REPLICATED):
                                    join (INNER, REPLICATED):
                                        alternatives
                                            dynamic filter ([ss_customer_sk_20::EQUAL, ss_item_sk_19::EQUAL], await [ss_sold_date_sk_40::EQUAL, ss_store_sk_24::EQUAL])
                                                scan store_sales
                                            cache data
                                                dynamic filter ([ss_customer_sk_20::EQUAL, ss_item_sk_19::EQUAL], await [ss_sold_date_sk_40::EQUAL, ss_store_sk_24::EQUAL])
                                                    scan store_sales
                                            load from cache
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPLICATE, BROADCAST, [])
                                                alternatives
                                                    scan date_dim
                                                    cache data
                                                        scan date_dim
                                                    load from cache
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            alternatives
                                                scan store
                                                cache data
                                                    scan store
                                                load from cache
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPLICATE, BROADCAST, [])
                                        alternatives
                                            scan item
                                            cache data
                                                scan item
                                            load from cache
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPARTITION, HASH, [c_customer_sk_107])
                                join (INNER, REPLICATED):
                                    alternatives
                                        dynamic filter ([c_current_addr_sk_111::EQUAL])
                                            scan customer
                                        cache data
                                            dynamic filter ([c_current_addr_sk_111::EQUAL])
                                                scan customer
                                        load from cache
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            alternatives
                                                scan customer_address
                                                cache data
                                                    scan customer_address
                                                load from cache
