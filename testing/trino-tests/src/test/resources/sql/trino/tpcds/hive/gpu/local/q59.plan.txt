- OutputNode
  - ProjectNode
    - TopNNode (GPU)
      - ExchangeNode (GPU)
        - TopNNode (GPU)
          - JoinNode (GPU)
            - AggregationNode
              - ProjectNode (GPU)
                - AggregationNode (GPU)
                  - ExchangeNode
                    - AggregationNode
                      - JoinNode (GPU)
                        - AggregationNode (GPU)
                          - FilterNode (GPU)
                            - TableScanNode (GPU)
                        - ExchangeNode (GPU)
                          - FilterNode (GPU)
                            - TableScanNode (GPU)
            - ExchangeNode (GPU)
              - JoinNode (GPU)
                - JoinNode (GPU)
                  - JoinNode (GPU)
                    - ProjectNode
                      - AggregationNode
                        - ProjectNode (GPU)
                          - AggregationNode (GPU)
                            - ExchangeNode
                              - AggregationNode
                                - JoinNode (GPU)
                                  - AggregationNode (GPU)
                                    - FilterNode (GPU)
                                      - TableScanNode (GPU)
                                  - ExchangeNode (GPU)
                                    - FilterNode (GPU)
                                      - TableScanNode (GPU)
                    - ExchangeNode (GPU)
                      - ProjectNode (GPU)
                        - FilterNode
                          - TableScanNode (GPU)
                  - ExchangeNode (GPU)
                    - ProjectNode (GPU)
                      - FilterNode
                        - TableScanNode (GPU)
                - ExchangeNode (GPU)
                  - JoinNode (GPU)
                    - FilterNode (GPU)
                      - TableScanNode (GPU)
                    - ExchangeNode (GPU)
                      - TableScanNode (GPU)
