- OutputNode
  - TopNNode (GPU)
    - ExchangeNode LOCAL
      - ExchangeNode REMOTE
        - TopNNode (GPU)
          - ExchangeNode LOCAL (GPU)
            - TopNNode (GPU)
              - ExchangeNode LOCAL (GPU)
                - TopNNode (GPU)
                  - ProjectNode
                    - AggregationNode
                      - ExchangeNode LOCAL
                        - ExchangeNode REMOTE
                          - AggregationNode
                            - ProjectNode (GPU)
                              - GroupIdNode (GPU)
                                - ProjectNode (GPU)
                                  - JoinNode (GPU)
                                    - ExchangeNode REMOTE
                                      - JoinNode (GPU)
                                        - ExchangeNode REMOTE
                                          - FilterNode (GPU)
                                            - TableScanNode inventory (GPU)
                                        - ExchangeNode LOCAL
                                          - ExchangeNode REMOTE
                                            - ProjectNode (GPU)
                                              - FilterNode (GPU)
                                                - TableScanNode date_dim (GPU)
                                    - ExchangeNode LOCAL
                                      - ExchangeNode REMOTE
                                        - TableScanNode item (GPU)
