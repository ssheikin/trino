- OutputNode
  - TopNNode (GPU)
    - ExchangeNode LOCAL
      - ExchangeNode REMOTE
        - TopNNode (GPU)
          - ExchangeNode LOCAL (GPU)
            - TopNNode (GPU)
              - ExchangeNode LOCAL (GPU)
                - TopNNode (GPU)
                  - AggregationNode (GPU)
                    - ExchangeNode LOCAL
                      - ExchangeNode REMOTE
                        - AggregationNode
                          - JoinNode (GPU)
                            - ExchangeNode REMOTE
                              - AggregationNode
                                - JoinNode (GPU)
                                  - ExchangeNode REMOTE
                                    - AggregationNode (GPU)
                                      - FilterNode (GPU)
                                        - TableScanNode store_sales (GPU)
                                  - ExchangeNode LOCAL
                                    - ExchangeNode REMOTE
                                      - ProjectNode (GPU)
                                        - FilterNode
                                          - TableScanNode item (GPU)
                            - ExchangeNode LOCAL
                              - ExchangeNode REMOTE
                                - ProjectNode (GPU)
                                  - FilterNode
                                    - TableScanNode date_dim (GPU)
