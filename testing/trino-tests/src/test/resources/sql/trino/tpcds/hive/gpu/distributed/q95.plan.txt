- OutputNode
  - AggregationNode (GPU)
    - ExchangeNode
      - ExchangeNode
        - AggregationNode (GPU)
          - ProjectNode (GPU)
            - AggregationNode (GPU)
              - ExchangeNode
                - ExchangeNode
                  - AggregationNode (GPU)
                    - FilterNode (GPU)
                      - GroupIdNode (GPU)
                        - JoinNode (GPU)
                          - AggregationNode (GPU)
                            - ExchangeNode (GPU)
                              - AggregationNode (GPU)
                                - JoinNode (GPU)
                                  - JoinNode (GPU)
                                    - ExchangeNode
                                      - FilterNode (GPU)
                                        - TableScanNode (GPU)
                                    - ExchangeNode
                                      - ExchangeNode
                                        - FilterNode (GPU)
                                          - TableScanNode (GPU)
                                  - ExchangeNode
                                    - ExchangeNode
                                      - FilterNode (GPU)
                                        - TableScanNode (GPU)
                          - JoinNode (GPU)
                            - AggregationNode (GPU)
                              - ExchangeNode (GPU)
                                - AggregationNode (GPU)
                                  - JoinNode (GPU)
                                    - ExchangeNode
                                      - FilterNode (GPU)
                                        - TableScanNode (GPU)
                                    - ExchangeNode
                                      - ExchangeNode
                                        - FilterNode (GPU)
                                          - TableScanNode (GPU)
                            - ExchangeNode
                              - ExchangeNode
                                - JoinNode (GPU)
                                  - ExchangeNode
                                    - JoinNode (GPU)
                                      - ExchangeNode
                                        - JoinNode (GPU)
                                          - ExchangeNode
                                            - FilterNode (GPU)
                                              - TableScanNode (GPU)
                                          - ExchangeNode
                                            - ExchangeNode
                                              - ProjectNode (GPU)
                                                - FilterNode
                                                  - TableScanNode (GPU)
                                      - ExchangeNode
                                        - ExchangeNode
                                          - ProjectNode (GPU)
                                            - FilterNode
                                              - TableScanNode (GPU)
                                  - ExchangeNode
                                    - ExchangeNode
                                      - ProjectNode (GPU)
                                        - FilterNode
                                          - TableScanNode (GPU)
