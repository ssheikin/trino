- OutputNode
  - TopNNode (GPU)
    - ExchangeNode (GPU)
      - TopNNode (GPU)
        - ProjectNode
          - AggregationNode
            - ExchangeNode
              - AggregationNode
                - GroupIdNode (GPU)
                  - ProjectNode (GPU)
                    - JoinNode (GPU)
                      - JoinNode (GPU)
                        - JoinNode (GPU)
                          - JoinNode (GPU)
                            - FilterNode (GPU)
                              - TableScanNode (GPU)
                            - ExchangeNode (GPU)
                              - JoinNode (GPU)
                                - FilterNode (GPU)
                                  - TableScanNode (GPU)
                                - ExchangeNode (GPU)
                                  - JoinNode (GPU)
                                    - ProjectNode (GPU)
                                      - FilterNode
                                        - TableScanNode (GPU)
                                    - ExchangeNode (GPU)
                                      - FilterNode (GPU)
                                        - TableScanNode (GPU)
                          - ExchangeNode (GPU)
                            - ProjectNode (GPU)
                              - FilterNode
                                - TableScanNode (GPU)
                        - ExchangeNode (GPU)
                          - ProjectNode (GPU)
                            - FilterNode
                              - TableScanNode (GPU)
                      - ExchangeNode (GPU)
                        - TableScanNode (GPU)
