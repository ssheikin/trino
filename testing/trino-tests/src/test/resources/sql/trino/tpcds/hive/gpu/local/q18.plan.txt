- OutputNode
  - TopNNode (GPU)
    - ExchangeNode LOCAL (GPU)
      - TopNNode (GPU)
        - ProjectNode
          - AggregationNode
            - ExchangeNode LOCAL
              - AggregationNode
                - GroupIdNode (GPU)
                  - ProjectNode (GPU)
                    - JoinNode (GPU)
                      - JoinNode (GPU)
                        - JoinNode (GPU)
                          - JoinNode (GPU)
                            - FilterNode (GPU)
                              - TableScanNode catalog_sales (GPU)
                            - ExchangeNode LOCAL (GPU)
                              - JoinNode (GPU)
                                - FilterNode (GPU)
                                  - TableScanNode customer_demographics (GPU)
                                - ExchangeNode LOCAL (GPU)
                                  - JoinNode (GPU)
                                    - ProjectNode (GPU)
                                      - FilterNode
                                        - TableScanNode customer (GPU)
                                    - ExchangeNode LOCAL
                                      - FilterNode
                                        - TableScanNode customer_address
                          - ExchangeNode LOCAL (GPU)
                            - ProjectNode (GPU)
                              - FilterNode
                                - TableScanNode date_dim (GPU)
                        - ExchangeNode LOCAL (GPU)
                          - ProjectNode (GPU)
                            - FilterNode
                              - TableScanNode customer_demographics (GPU)
                      - ExchangeNode LOCAL (GPU)
                        - TableScanNode item (GPU)
