- OutputNode
  - TopNNode (GPU)
    - ExchangeNode (GPU)
      - TopNNode (GPU)
        - JoinNode
          - JoinNode (GPU)
            - FilterNode (GPU)
              - ProjectNode (GPU)
                - AggregationNode (GPU)
                  - ExchangeNode
                    - AggregationNode
                      - JoinNode (GPU)
                        - JoinNode (GPU)
                          - AggregationNode (GPU)
                            - FilterNode (GPU)
                              - TableScanNode (GPU)
                          - ExchangeNode (GPU)
                            - FilterNode (GPU)
                              - TableScanNode (GPU)
                        - ExchangeNode (GPU)
                          - FilterNode (GPU)
                            - TableScanNode (GPU)
            - ExchangeNode (GPU)
              - JoinNode (GPU)
                - ProjectNode (GPU)
                  - AggregationNode (GPU)
                    - ExchangeNode
                      - AggregationNode
                        - JoinNode (GPU)
                          - FilterNode (GPU)
                            - TableScanNode (GPU)
                          - ExchangeNode
                            - AggregationNode
                              - JoinNode (GPU)
                                - AggregationNode (GPU)
                                  - FilterNode (GPU)
                                    - TableScanNode (GPU)
                                - ExchangeNode (GPU)
                                  - FilterNode (GPU)
                                    - TableScanNode (GPU)
                - ExchangeNode (GPU)
                  - FilterNode (GPU)
                    - ProjectNode (GPU)
                      - AggregationNode (GPU)
                        - ExchangeNode
                          - AggregationNode
                            - JoinNode (GPU)
                              - FilterNode (GPU)
                                - TableScanNode (GPU)
                              - ExchangeNode
                                - AggregationNode
                                  - JoinNode (GPU)
                                    - AggregationNode (GPU)
                                      - FilterNode (GPU)
                                        - TableScanNode (GPU)
                                    - ExchangeNode (GPU)
                                      - FilterNode (GPU)
                                        - TableScanNode (GPU)
          - ExchangeNode
            - ProjectNode (GPU)
              - AggregationNode (GPU)
                - ExchangeNode
                  - AggregationNode
                    - JoinNode (GPU)
                      - JoinNode (GPU)
                        - AggregationNode (GPU)
                          - FilterNode (GPU)
                            - TableScanNode (GPU)
                        - ExchangeNode (GPU)
                          - FilterNode (GPU)
                            - TableScanNode (GPU)
                      - ExchangeNode (GPU)
                        - TableScanNode (GPU)
