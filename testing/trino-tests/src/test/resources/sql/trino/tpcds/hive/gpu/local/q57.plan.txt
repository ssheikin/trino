- OutputNode
  - ProjectNode (GPU)
    - TopNNode (GPU)
      - ExchangeNode (GPU)
        - TopNNode (GPU)
          - ProjectNode
            - JoinNode
              - ProjectNode (GPU)
                - JoinNode (GPU)
                  - FilterNode
                    - WindowNode
                      - FilterNode
                        - WindowNode
                          - AggregationNode (GPU)
                            - ExchangeNode
                              - AggregationNode
                                - JoinNode (GPU)
                                  - JoinNode (GPU)
                                    - JoinNode (GPU)
                                      - AggregationNode (GPU)
                                        - FilterNode (GPU)
                                          - TableScanNode (GPU)
                                      - ExchangeNode (GPU)
                                        - FilterNode (GPU)
                                          - TableScanNode (GPU)
                                    - ExchangeNode (GPU)
                                      - FilterNode (GPU)
                                        - TableScanNode (GPU)
                                  - ExchangeNode (GPU)
                                    - FilterNode (GPU)
                                      - TableScanNode (GPU)
                  - ExchangeNode
                    - ProjectNode
                      - ProjectNode
                        - WindowNode
                          - AggregationNode (GPU)
                            - ExchangeNode
                              - AggregationNode
                                - JoinNode (GPU)
                                  - JoinNode (GPU)
                                    - JoinNode (GPU)
                                      - AggregationNode (GPU)
                                        - FilterNode (GPU)
                                          - TableScanNode (GPU)
                                      - ExchangeNode (GPU)
                                        - FilterNode (GPU)
                                          - TableScanNode (GPU)
                                    - ExchangeNode (GPU)
                                      - FilterNode (GPU)
                                        - TableScanNode (GPU)
                                  - ExchangeNode (GPU)
                                    - FilterNode (GPU)
                                      - TableScanNode (GPU)
              - ExchangeNode
                - ProjectNode
                  - ProjectNode
                    - WindowNode
                      - AggregationNode (GPU)
                        - ExchangeNode
                          - AggregationNode
                            - JoinNode (GPU)
                              - JoinNode (GPU)
                                - JoinNode (GPU)
                                  - AggregationNode (GPU)
                                    - FilterNode (GPU)
                                      - TableScanNode (GPU)
                                  - ExchangeNode (GPU)
                                    - FilterNode (GPU)
                                      - TableScanNode (GPU)
                                - ExchangeNode (GPU)
                                  - TableScanNode (GPU)
                              - ExchangeNode (GPU)
                                - TableScanNode (GPU)
