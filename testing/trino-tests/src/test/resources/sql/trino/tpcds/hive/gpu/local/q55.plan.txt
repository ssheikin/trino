- OutputNode
  - TopNNode (GPU)
    - ExchangeNode LOCAL (GPU)
      - TopNNode (GPU)
        - AggregationNode (GPU)
          - ExchangeNode LOCAL (GPU)
            - AggregationNode (GPU)
              - JoinNode (GPU)
                - JoinNode (GPU)
                  - FilterNode (GPU)
                    - TableScanNode store_sales (GPU)
                  - ExchangeNode LOCAL (GPU)
                    - ProjectNode (GPU)
                      - FilterNode (GPU)
                        - TableScanNode item (GPU)
                - ExchangeNode LOCAL (GPU)
                  - ProjectNode (GPU)
                    - FilterNode (GPU)
                      - TableScanNode date_dim (GPU)
