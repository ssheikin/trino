- OutputNode
  - ExchangeNode REMOTE
    - ExchangeNode LOCAL
      - SortNode
        - ExchangeNode REMOTE
          - AggregationNode (GPU)
            - ExchangeNode LOCAL
              - ExchangeNode REMOTE
                - AggregationNode (GPU)
                  - ProjectNode (GPU)
                    - JoinNode (GPU)
                      - ExchangeNode REMOTE
                        - FilterNode (GPU)
                          - TableScanNode orders (GPU)
                      - ExchangeNode LOCAL
                        - ExchangeNode REMOTE
                          - ProjectNode (GPU)
                            - FilterNode (GPU)
                              - TableScanNode lineitem (GPU)
