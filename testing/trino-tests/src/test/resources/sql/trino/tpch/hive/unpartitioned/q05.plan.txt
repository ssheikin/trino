remote exchange (GATHER, SINGLE, [])
    local exchange (GATHER, UNKNOWN, [])
        remote exchange (REPARTITION, ROUND_ROBIN, [])
            final aggregation over (name_21)
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, [name_21])
                        partial aggregation over (name_21)
                            join (INNER, PARTITIONED):
                                remote exchange (REPARTITION, HASH, [nationkey_13, orderkey_5])
                                    join (INNER, REPLICATED):
                                        dynamic filter ([orderkey_5::bigint, suppkey::bigint])
                                            scan lineitem
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPLICATE, BROADCAST, [])
                                                join (INNER, REPLICATED):
                                                    dynamic filter (await [nationkey_13::bigint, nationkey_13::bigint])
                                                        scan supplier
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            join (INNER, REPLICATED):
                                                                dynamic filter (await [nationkey_20::bigint, regionkey::bigint])
                                                                    scan nation
                                                                local exchange (GATHER, SINGLE, [])
                                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                                        scan region
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [nationkey, orderkey])
                                        join (INNER, PARTITIONED):
                                            remote exchange (REPARTITION, HASH, [custkey_0])
                                                dynamic filter ([custkey_0::bigint])
                                                    scan orders
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPARTITION, HASH, [custkey])
                                                    scan customer
