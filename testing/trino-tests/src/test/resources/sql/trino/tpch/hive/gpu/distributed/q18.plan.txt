- OutputNode
  - TopNNode (GPU)
    - ExchangeNode
      - ExchangeNode
        - TopNNode (GPU)
          - ExchangeNode (GPU)
            - TopNNode (GPU)
              - ExchangeNode (GPU)
                - TopNNode (GPU)
                  - AggregationNode (GPU)
                    - ExchangeNode (GPU)
                      - JoinNode (GPU)
                        - ExchangeNode
                          - AggregationNode (GPU)
                            - FilterNode (GPU)
                              - TableScanNode (GPU)
                        - ExchangeNode
                          - ExchangeNode
                            - JoinNode (GPU)
                              - ExchangeNode
                                - JoinNode (GPU)
                                  - ExchangeNode
                                    - FilterNode (GPU)
                                      - TableScanNode (GPU)
                                  - ProjectNode (GPU)
                                    - FilterNode
                                      - AggregationNode (GPU)
                                        - ExchangeNode
                                          - ExchangeNode
                                            - AggregationNode (GPU)
                                              - TableScanNode (GPU)
                              - ExchangeNode
                                - ExchangeNode
                                  - TableScanNode (GPU)
