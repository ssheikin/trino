- OutputNode
  - TopNNode (GPU)
    - ExchangeNode LOCAL (GPU)
      - TopNNode (GPU)
        - AggregationNode (GPU)
          - ExchangeNode LOCAL (GPU)
            - JoinNode (GPU)
              - AggregationNode (GPU)
                - FilterNode (GPU)
                  - TableScanNode lineitem (GPU)
              - ExchangeNode LOCAL (GPU)
                - JoinNode (GPU)
                  - JoinNode (GPU)
                    - FilterNode (GPU)
                      - TableScanNode orders (GPU)
                    - ProjectNode (GPU)
                      - FilterNode (GPU)
                        - AggregationNode (GPU)
                          - ExchangeNode LOCAL (GPU)
                            - AggregationNode (GPU)
                              - TableScanNode lineitem (GPU)
                  - ExchangeNode LOCAL (GPU)
                    - TableScanNode customer (GPU)
