- OutputNode
  - AggregationNode (GPU)
    - ExchangeNode LOCAL (GPU)
      - AggregationNode (GPU)
        - ProjectNode
          - JoinNode
            - ProjectNode (GPU)
              - FilterNode (GPU)
                - TableScanNode lineitem (GPU)
            - ExchangeNode LOCAL (GPU)
              - FilterNode (GPU)
                - TableScanNode part (GPU)
