- OutputNode
  - ExchangeNode LOCAL
    - SortNode
      - ProjectNode
        - FilterNode
          - JoinNode
            - AggregationNode (GPU)
              - ExchangeNode LOCAL (GPU)
                - AggregationNode (GPU)
                  - ProjectNode (GPU)
                    - JoinNode (GPU)
                      - FilterNode (GPU)
                        - TableScanNode partsupp (GPU)
                      - ExchangeNode LOCAL (GPU)
                        - JoinNode (GPU)
                          - FilterNode (GPU)
                            - TableScanNode supplier (GPU)
                          - ExchangeNode LOCAL (GPU)
                            - ProjectNode (GPU)
                              - FilterNode (GPU)
                                - TableScanNode nation (GPU)
            - ExchangeNode LOCAL
              - ProjectNode
                - ExchangeNode LOCAL (GPU)
                  - AggregationNode (GPU)
                    - ExchangeNode LOCAL (GPU)
                      - AggregationNode (GPU)
                        - ProjectNode (GPU)
                          - JoinNode (GPU)
                            - FilterNode (GPU)
                              - TableScanNode partsupp (GPU)
                            - ExchangeNode LOCAL (GPU)
                              - JoinNode (GPU)
                                - FilterNode (GPU)
                                  - TableScanNode supplier (GPU)
                                - ExchangeNode LOCAL (GPU)
                                  - ProjectNode (GPU)
                                    - FilterNode (GPU)
                                      - TableScanNode nation (GPU)
