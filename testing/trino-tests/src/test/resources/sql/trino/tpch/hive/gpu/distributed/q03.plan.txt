- OutputNode
  - TopNNode (GPU)
    - ExchangeNode LOCAL
      - ExchangeNode REMOTE
        - TopNNode (GPU)
          - ExchangeNode LOCAL (GPU)
            - TopNNode (GPU)
              - ExchangeNode LOCAL (GPU)
                - TopNNode (GPU)
                  - AggregationNode (GPU)
                    - ExchangeNode LOCAL (GPU)
                      - JoinNode (GPU)
                        - AggregationNode (GPU)
                          - ProjectNode
                            - ExchangeNode REMOTE
                              - ProjectNode (GPU)
                                - FilterNode
                                  - TableScanNode lineitem (GPU)
                        - ExchangeNode LOCAL
                          - ExchangeNode REMOTE
                            - JoinNode (GPU)
                              - ExchangeNode REMOTE
                                - FilterNode (GPU)
                                  - TableScanNode orders (GPU)
                              - ExchangeNode LOCAL
                                - ExchangeNode REMOTE
                                  - ProjectNode (GPU)
                                    - FilterNode
                                      - TableScanNode customer (GPU)
