- OutputNode
  - TopNNode (GPU)
    - ExchangeNode LOCAL (GPU)
      - TopNNode (GPU)
        - AggregationNode (GPU)
          - ExchangeNode LOCAL (GPU)
            - JoinNode (GPU)
              - AggregationNode (GPU)
                - ProjectNode (GPU)
                  - ProjectNode (GPU)
                    - FilterNode
                      - TableScanNode lineitem (GPU)
              - ExchangeNode LOCAL (GPU)
                - JoinNode (GPU)
                  - FilterNode (GPU)
                    - TableScanNode orders (GPU)
                  - ExchangeNode LOCAL (GPU)
                    - ProjectNode (GPU)
                      - FilterNode
                        - TableScanNode customer (GPU)
