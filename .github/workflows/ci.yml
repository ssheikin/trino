name: ci

on:
  push:
    branches:
      - master
      - release-*-e
  pull_request:
  merge_group:
  repository_dispatch:
    types: [test-with-secrets-command]
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *" # nightly
    - cron: "0 0 * * 6" # every Saturday

defaults:
  run:
    shell: bash --noprofile --norc -euo pipefail {0}

env:
  # An envar that signals to tests we are executing in the CI environment
  CONTINUOUS_INTEGRATION: true
  # allow overriding Maven command
  MAVEN: ./mvnw -s .github/settings.xml
  MAVEN_OPTS: "-Xmx512M -XX:+ExitOnOutOfMemoryError"
  MAVEN_INSTALL_OPTS: "-Xmx3G -XX:+ExitOnOutOfMemoryError"
  MAVEN_FAST_INSTALL: "-B -V -T 1C -DskipTests -Dmaven.source.skip=true -Dair.check.skip-all"
  MAVEN_COMPILE_COMMITS: "-B --quiet -T 1C -DskipTests -Dmaven.source.skip=true -Dair.check.skip-all=true -Dmaven.javadoc.skip=true --no-snapshot-updates --no-transfer-progress"
  MAVEN_GIB: "-P gib -Dgib.referenceBranch=refs/remotes/origin/${{ github.event_name == 'pull_request' && github.event.pull_request.base.ref || github.event.repository.default_branch }}"
  MAVEN_TEST: "-B -Dmaven.source.skip=true -Dair.check.skip-all --fail-at-end -P gib -Dgib.referenceBranch=refs/remotes/origin/${{ github.event_name == 'pull_request' && github.event.pull_request.base.ref || github.event.repository.default_branch }}"
  # Testcontainers kills image pulls if they don't make progress for > 30s and retries for 2m before failing. This means
  # that if an image doesn't download all it's layers within ~2m then any other concurrent pull will be killed because
  # the Docker daemon only downloads 3 layers concurrently which prevents the other pull from making any progress.
  # This value should be greater than the time taken for the longest image pull.
  TESTCONTAINERS_PULL_PAUSE_TIMEOUT: 600
  # Use Starburst pull-through cache for Docker images
  STARBURST_DOCKER_PULL_THROUGH_REGISTRY: 843985043183.dkr.ecr.us-east-1.amazonaws.com/docker-hub/
  # used by actions/cache to retry the download after this time: https://github.com/actions/cache/blob/main/workarounds.md#cache-segment-restore-timeout
  SEGMENT_DOWNLOAD_TIMEOUT_MINS: 5
  CI_SKIP_SECRETS_PRESENCE_CHECKS: ${{ secrets.CI_SKIP_SECRETS_PRESENCE_CHECKS }}
  SECRETS_PRESENT: ${{ secrets.SECRETS_PRESENT }}
  PTL_TMP_DOWNLOAD_PATH: /tmp/pt_java_downloads

# Cancel previous PR builds.
concurrency:
  # Cancel all workflow runs except latest within a concurrency group. This is achieved by defining a concurrency group for the PR.
  # Non-PR builds have singleton concurrency groups.
  # When triggered by the repository_dispatch, add the expected SHA to avoid cancelling the run from the PR.
  group: |
    workflow=${{ github.workflow }},
    pr_number=${{ github.event_name == 'pull_request' && github.event.number || 'NA' }},
    dispatch_sha=${{ github.event_name == 'repository_dispatch' && github.event.client_payload.slash_command.args.named.sha || 'NA' }},
    commit_sha=${{ github.event_name != 'pull_request' && github.event_name != 'repository_dispatch' && github.sha || 'NA' }}
  cancel-in-progress: true
jobs:
  path-filters:
    runs-on: [self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest]
    outputs:
      docs: ${{ steps.filter.outputs.docs }}
      non_docs: ${{ steps.filter.outputs.non_docs }}
    steps:
      - uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d # v4
        id: filter
        if: github.event_name == 'pull_request'
        with:
          # Note: `docs` output is currently unused. The docs changes are tested by maven-checks job, leveraging GIB impact analysis.
          # TODO remove use of non_docs filters and remove `path-filters` job. Use GIB impact analysis to guard job skipping.
          filters: |
            docs: 'docs/**'
            non_docs: '!docs/**'

  maven-checks:
    runs-on: [self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest]
    name: maven-checks ${{ matrix.java-version }}
    strategy:
      fail-fast: ${{ github.event_name == 'merge_group' }}
      matrix:
        include:
          - { java-version: 25.0.3, cache: 'true', cleanup-node: true }
          - { java-version: 26, cache: 'restore', cleanup-node: true }
          - { java-version: 27-ea, cache: 'restore', cleanup-node: true }
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits, as the build result depends on `git describe` equivalent
          ref: |
            ${{ github.event_name == 'repository_dispatch' &&
                github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha &&
                format('refs/pull/{0}/head', github.event.client_payload.pull_request.number) || '' }}
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
      - uses: ./.github/actions/setup
        timeout-minutes: 15
        with:
          cache: ${{ matrix.cache }}
          java-version: ${{ matrix.java-version }}
          cleanup-node: true
      - name: Check SPI backward compatibility
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} -pl :trino-spi -am
          $MAVEN clean verify -B --strict-checksums -DskipTests -pl :trino-spi
      - name: Maven Checks
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean verify -B --strict-checksums -V -T 1C -DskipTests -P ci
      - name: Remove Trino from local Maven repo to avoid caching it
        # Avoid caching artifacts built in this job, cache should only include dependencies
        if: steps.cache.outputs.cache-hit != 'true' && matrix.cache == 'true'
        run: rm -rf ~/.m2/repository/io/trino/trino-*
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "::error::Cancelling merge queue workflow because maven-checks ${{ matrix.java-version }} failed"
          gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  artifact-checks:
    needs: path-filters
    if: github.event_name != 'pull_request' || needs.path-filters.outputs.non_docs == 'true'
    runs-on: [self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest]
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits, as the build result depends on `git describe` equivalent
          ref: |
            ${{ github.event_name == 'repository_dispatch' &&
                github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha &&
                format('refs/pull/{0}/head', github.event.client_payload.pull_request.number) || '' }}
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
      - uses: ./.github/actions/setup
        timeout-minutes: 15
        with:
          cache: 'restore'
          cleanup-node: 'true'
      - name: "Login to ECR pull-through cache"
        shell: bash
        run: |
          aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 843985043183.dkr.ecr.us-east-1.amazonaws.com
      - name: Maven Install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} -pl '!:trino-docs'
      - name: Test JDBC shading
        # Run only integration tests to verify JDBC driver shading
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN failsafe:integration-test failsafe:verify -B --strict-checksums -P ci -pl :trino-jdbc
      - name: Clean Maven Output
        run: $MAVEN clean -pl '!:trino-server,!:trino-cli'
      - uses: docker/setup-qemu-action@ce360397dd3f832beb865e1373c09c0e9f86d70a # v4
        with:
          image: 843985043183.dkr.ecr.us-east-1.amazonaws.com/docker-hub/tonistiigi/binfmt:latest
          platforms: arm64,ppc64le
      - name: Build and Test Docker Image
        run: core/docker/build.sh
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "::error::Cancelling merge queue workflow because artifact-checks failed"
          gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  check-commits-dispatcher:
    needs: path-filters
    if: github.event_name == 'pull_request' && needs.path-filters.outputs.non_docs == 'true'
    runs-on: [self-hosted, gha-fleet-ec2-2cpu-8gb-amd64, latest]
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits to be able to determine merge base
      - name: Block illegal commits
        uses: trinodb/github-actions/block-commits@c2991972560c5219d9ae5fb68c0c9d687ffcdd10
        with:
          action-merge: fail
          action-fixup: none
      - name: Set matrix (dispatch commit checks)
        id: set-matrix
        run: |
          # Make sure the PR branch contains the compile-commit composite job
          if git merge-base --is-ancestor $( git rev-list HEAD -- .github/actions/compile-commit/action.yml | tail -n 1 ) ${{ github.event.pull_request.head.sha }}
          then
            # The HEAD commit of the PR can be safely ignored since it's already compiled in other jobs
            # This is achieved by adding a tilde (~) after the HEAD sha
            git log --reverse --pretty=format:'%H,%T,"%s"' refs/remotes/origin/${{ github.event.pull_request.base.ref }}..${{ github.event.pull_request.head.sha }}~ | ./.github/bin/prepare-check-commits-matrix.py > commit-matrix.json
          else
            echo -n '' > commit-matrix.json
          fi

          echo "Commit matrix: $(jq '.' commit-matrix.json)"
          echo "matrix=$(jq -c '.' commit-matrix.json)" >> $GITHUB_OUTPUT

  check-commit:
    needs: check-commits-dispatcher
    runs-on: [self-hosted, gha-fleet-ec2-8cpu-64gb-amd64, latest]
    timeout-minutes: 30
    if: github.event_name == 'pull_request' && needs.check-commits-dispatcher.outputs.matrix != ''
    strategy:
      fail-fast: ${{ github.event_name == 'merge_group' }}
      matrix: ${{ fromJson(needs.check-commits-dispatcher.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v6
        if: matrix.commit != ''
        with:
          fetch-depth: 0 # checkout all commits to be able to determine merge base
          ref: ${{ matrix.commit }}
      - name: Set up AWS code-artifacts login
        if: matrix.commit != ''
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
      # This composite job must be entirely standalone, and checked out from the correct commit before being executed.
      # It can't accept any parameters defined in this workflow, because the values of those parameters would always be taken from
      # PR HEAD since that is the commit the workflow is started for. This could lead to problems if those parameters were changed
      # in the middle of a PR branch.
      - uses: ./.github/actions/compile-commit
        if: matrix.commit != ''
        with:
          base_ref: ${{ github.event.pull_request.base.ref }}

  error-prone-checks:
    needs: path-filters
    if: github.event_name != 'pull_request' || needs.path-filters.outputs.non_docs == 'true'
    runs-on: [self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest]
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits to be able to determine merge base for GIB
          ref: |
            ${{ github.event_name == 'repository_dispatch' &&
                github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha &&
                format('refs/pull/{0}/head', github.event.client_payload.pull_request.number) || '' }}
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
      - uses: ./.github/actions/setup
        timeout-minutes: 15
        with:
          cache: restore
          cleanup-node: true # fix "No space left on device" error
          java-version: 25.0.3
      - name: Maven Install
        run: |
          # build everything to make sure dependencies of impacted modules are present
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} ${MAVEN_GIB} -pl '!:trino-docs,!:trino-server'
      - name: Error Prone Checks
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          # Skip checks, these are run in `maven-checks` job and e.g. checkstyle is expensive.
          $MAVEN ${MAVEN_TEST} -T 1C clean compile test-compile -DskipTests -Dair.check.skip-all=true ${MAVEN_GIB} -Dgib.buildUpstream=never -P errorprone-compiler \
            -pl '!:trino-docs,!:trino-server'
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "::error::Cancelling merge queue workflow because error-prone-checks failed"
          gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  test-jdbc-compatibility:
    needs: path-filters
    if: github.event_name != 'pull_request' || needs.path-filters.outputs.non_docs == 'true'
    runs-on: [self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest]
    timeout-minutes: 30
    env:
      REPORT_TEST_SESSION_NAME: modules
      REPORT_SERVICE_NAME: jdbc-compatibility
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout tags so version in Manifest is set properly
          ref: |
            ${{ github.event_name == 'repository_dispatch' &&
                github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha &&
                format('refs/pull/{0}/head', github.event.client_payload.pull_request.number) || '' }}
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
      - name: "Login to ECR pull-through cache"
        shell: bash
        run: |
          aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 843985043183.dkr.ecr.us-east-1.amazonaws.com
      - uses: ./.github/actions/setup
        timeout-minutes: 15
        with:
          cache: restore
          cleanup-node: 'true'
      - name: Maven Install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} ${MAVEN_GIB} -Dgib.logImpactedTo=gib-impacted.log -pl '!:trino-docs,!:trino-server'
      - name: Test old JDBC vs current server
        id: tests-old
        run: |
          if [ ! -f gib-impacted.log ] || grep -q testing/trino-test-jdbc-compatibility-old-driver gib-impacted.log; then
            testing/trino-test-jdbc-compatibility-old-driver/bin/run_tests.sh
          fi
      - name: Test current JDBC vs old server
        id: tests-current
        if: always()
        run: |
          if [ ! -f gib-impacted.log ] || grep -q testing/trino-test-jdbc-compatibility-old-server gib-impacted.log; then
            $MAVEN test ${MAVEN_TEST} -pl :trino-test-jdbc-compatibility-old-server
          fi
      - name: Upload test results
        uses: ./.github/actions/process-test-results
        if: always()
        with:
          has-failed-tests: ${{ steps.tests-old.outcome == 'failure' || steps.tests-current.outcome == 'failure' }}
          upload-heap-dump: ${{ env.SECRETS_PRESENT == '' && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
      - name: Upload test results to Datadog
        uses: ./.github/actions/upload-test-results-datadog
        if: always()
        with:
          datadog-api-key: ${{ secrets.DATADOG_API_KEY }}
      - name: Launch Flake Tracker
        if: failure() && github.ref == 'refs/heads/master' && (steps.tests-old.outcome == 'failure' || steps.tests-current.outcome == 'failure')
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "::error::Cancelling merge queue workflow because test-jdbc-compatibility failed"
          gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  hive-tests:
    needs: path-filters
    if: github.event_name != 'pull_request' || needs.path-filters.outputs.non_docs == 'true'
    runs-on: [self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest]
    strategy:
      fail-fast: ${{ github.event_name == 'merge_group' }}
      matrix:
        config:
          - config-hdp3
          # TODO: config-apache-hive3
    timeout-minutes: 60
    env:
      REPORT_TEST_SESSION_NAME: modules
      REPORT_SERVICE_NAME: hive-${{ matrix.config }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits to be able to determine merge base for GIB
          ref: |
            ${{ github.event_name == 'repository_dispatch' &&
                github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha &&
                format('refs/pull/{0}/head', github.event.client_payload.pull_request.number) || '' }}
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
          login-docker: true
      - uses: ./.github/actions/setup
        timeout-minutes: 15
        with:
          cache: restore
      - name: Install Hive Module
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} ${MAVEN_GIB} -Dgib.logImpactedTo=gib-impacted.log -am -pl :trino-hive
      - name: Run Hive Cloud Tests
        id: tests
        env:
          ABFS_CONTAINER: ${{ vars.AZURE_ABFS_CONTAINER }}
          ABFS_ACCOUNT: ${{ vars.AZURE_ABFS_ACCOUNT }}
          ABFS_ACCESSKEY: ${{ secrets.AZURE_ABFS_ACCESSKEY }}
          AWS_ACCESS_KEY_ID: ${{ vars.TRINO_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TRINO_AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ vars.TRINO_AWS_REGION }}
          S3_BUCKET: ${{ vars.TRINO_S3_BUCKET }}
          S3_BUCKET_ENDPOINT: "s3.${{ vars.TRINO_AWS_REGION }}.amazonaws.com"
          GCP_CREDENTIALS_KEY: ${{ secrets.GCP_CREDENTIALS_KEY }}
          GCP_STORAGE_BUCKET: ${{ vars.GCP_STORAGE_BUCKET }}
          GCS_DATABRICKS_UNITY_JDBC_URL: ${{ vars.GCS_DATABRICKS_UNITY_JDBC_URL }}
          GCS_DATABRICKS_UNITY_CATALOG_NAME: ${{ vars.GCS_DATABRICKS_UNITY_CATALOG_NAME }}
          GCS_DATABRICKS_UNITY_EXTERNAL_LOCATION: ${{ vars.GCS_DATABRICKS_UNITY_EXTERNAL_LOCATION }}
          GCS_DATABRICKS_HOST: ${{ vars.GCS_DATABRICKS_HOST }}
          GCS_DATABRICKS_LOGIN: token
          GCS_DATABRICKS_TOKEN: ${{ secrets.GCS_DATABRICKS_TOKEN }}
          DATABRICKS_UNITY_GCP_GCS_JSON_KEY: ${{ secrets.DATABRICKS_UNITY_GCP_GCS_JSON_KEY }}
          DATABRICKS_UNITY_JDBC_URL: ${{ vars.DATABRICKS_UNITY_JDBC_URL }}
          DATABRICKS_UNITY_CATALOG_NAME: ${{ vars.DATABRICKS_UNITY_CATALOG_NAME }}
          DATABRICKS_UNITY_EXTERNAL_LOCATION: ${{ vars.DATABRICKS_UNITY_EXTERNAL_LOCATION }}
          DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
          DATABRICKS_LOGIN: token
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          DATABRICKS_AWS_ACCESS_KEY_ID: ${{ vars.GALAXY_TRINO_DATABRICKS_AWS_ACCESS_KEY_ID }}
          DATABRICKS_AWS_SECRET_ACCESS_KEY: ${{ secrets.GALAXY_TRINO_DATABRICKS_AWS_SECRET_ACCESS_KEY }}
          DATABRICKS_AWS_REGION: us-east-1
        run: |
          if [ "${AWS_ACCESS_KEY_ID}" != "" ] && ( [ ! -f gib-impacted.log ] || grep -q plugin/trino-hive gib-impacted.log ); then
            $MAVEN test ${MAVEN_TEST} -pl :trino-hive -P cloud-tests
          fi
      - name: Run ABFS Tests
        id: test-hive-abfs
        env:
          ABFS_TESTS_ABFS_ACCOUNT: ${{ vars.AZURE_ABFS_ACCOUNT }}
          ABFS_TESTS_CLIENT_SECRET: ${{ env.ABFS_TESTS_CLIENT_SECRET }}
          ABFS_TESTS_ABFS_ACCESS_KEY: ${{ secrets.AZURE_ABFS_ACCESSKEY }}
        run: |
          if [ "${ABFS_TESTS_ABFS_ACCOUNT}" != "" ] && ( [ ! -f gib-impacted.log ] || grep -q plugin/trino-hive gib-impacted.log ); then
            $MAVEN test ${MAVEN_TEST} -pl :trino-hive -P abfs-tests \
              -Dhive.hadoop2.azure-abfs-account="${ABFS_TESTS_ABFS_ACCOUNT}" \
              -Dhive.hadoop2.azure-abfs-container=container \
              -Dhive.hadoop2.azure-abfs-access-key="${ABFS_TESTS_ABFS_ACCESS_KEY}" \
              -Dhive.hadoop2.azure-abfs-client-secret="${ABFS_TESTS_CLIENT_SECRET}"
          fi
      - name: Upload test results
        uses: ./.github/actions/process-test-results
        if: always()
        with:
          has-failed-tests: ${{ steps.tests.outcome == 'failure' }}
          upload-heap-dump: ${{ env.SECRETS_PRESENT == '' && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
      - name: Upload test results to Datadog
        uses: ./.github/actions/upload-test-results-datadog
        if: always()
        with:
          datadog-api-key: ${{ secrets.DATADOG_API_KEY }}
      - name: Update PR check
        uses: ./.github/actions/update-check
        if: >-
          failure() &&
          github.event_name == 'repository_dispatch' &&
          github.event.client_payload.slash_command.args.named.sha != '' &&
          github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha
        with:
          pull_request_number: ${{ github.event.client_payload.pull_request.number }}
          check_name: ${{ github.job }} (${{ matrix.config }}) with secrets
          conclusion: ${{ job.status }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - name: Launch Flake Tracker
        if: failure() && steps.tests.outcome == 'failure' && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  # ------------------------------------------------------------------------
  # Only run BigQuery tests on a schedule, labeled PRs, or workflow_dispatch
  # ------------------------------------------------------------------------
  bigquery-tests:
    name: bigquery-tests
    runs-on: [self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest]
    strategy:
      fail-fast: false
      matrix:
        profiles:
          - cloud-tests-1
          - cloud-tests-2
    timeout-minutes: 60
    if: "(github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'bigquery')) || (github.event_name == 'workflow_dispatch')"
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits, as the build result depends on `git describe` equivalent
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
      - name: "Login to ECR pull-through cache"
        shell: bash
        run: |
          aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 843985043183.dkr.ecr.us-east-1.amazonaws.com
      - uses: ./.github/actions/setup
      - name: Maven install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} -am -pl :trino-bigquery
      - name: Cloud BigQuery Tests
        id: tests-bq
        env:
          BIGQUERY_CREDENTIALS_KEY: ${{ secrets.BIGQUERY_CREDENTIALS_KEY }}
          BIGQUERY_CASE_INSENSITIVE_CREDENTIALS_KEY: ${{ secrets.BIGQUERY_CASE_INSENSITIVE_CREDENTIALS_KEY }}
          GCP_STORAGE_BUCKET: ${{ vars.GCP_STORAGE_BUCKET }}
          BIGQUERY_TESTING_BIGLAKE_CONNECTION_ID: ${{ vars.BIGQUERY_TESTING_BIGLAKE_CONNECTION_ID }}
          BIGQUERY_TESTING_PROJECT_ID: ${{ vars.BIGQUERY_TESTING_PROJECT_ID }}
          BIGQUERY_TESTING_PARENT_PROJECT_ID: ${{ vars.BIGQUERY_TESTING_PARENT_PROJECT_ID }}
        if: contains(matrix.profiles, 'cloud-tests-1')
        run: |
          $MAVEN test ${MAVEN_TEST} -pl :trino-bigquery -Pcloud-tests-1 \
            -Dtesting.bigquery.credentials-key="${BIGQUERY_CREDENTIALS_KEY}" \
            -Dtesting.bigquery-case-insensitive.credentials-key="${BIGQUERY_CASE_INSENSITIVE_CREDENTIALS_KEY}" \
            -Dtesting.gcp-storage-bucket="${GCP_STORAGE_BUCKET}" \
            -Dtesting.bigquery-connection-id="${BIGQUERY_TESTING_BIGLAKE_CONNECTION_ID}" \
            -Dtesting.bigquery-project-id="${BIGQUERY_TESTING_PROJECT_ID}" \
            -Dtesting.bigquery-parent-project-id="${BIGQUERY_TESTING_PARENT_PROJECT_ID}"
      - name: Cloud BigQuery Smoke Tests
        id: tests-bq-smoke
        env:
          BIGQUERY_CREDENTIALS_KEY: ${{ secrets.BIGQUERY_CREDENTIALS_KEY }}
          GCP_STORAGE_BUCKET: ${{ vars.GCP_STORAGE_BUCKET }}
        if: contains(matrix.profiles, 'cloud-tests-2')
        run: |
          $MAVEN test ${MAVEN_TEST} -pl :trino-bigquery -Pcloud-tests-2 \
            -Dtesting.bigquery.credentials-key="${BIGQUERY_CREDENTIALS_KEY}" \
            -Dtesting.gcp-storage-bucket="${GCP_STORAGE_BUCKET}" \
            -Dtesting.alternate-bq-project-id=bigquery-cicd-alternate
      - name: Sanitize artifact name
        if: always()
        run: |
          # Generate a valid artifact name and make it available to next steps as
          # an environment variable ARTIFACT_NAME
          # ", :, <, >, |, *, ?, \, / are not allowed in artifact names, replace it with an underscore
          name=$(echo -n "plugin/trino-bigquery, ${{ matrix.profiles }}" | sed -e 's/[":<>|\*\?\\\/]/_/g')
          echo "ARTIFACT_NAME=$name" >> $GITHUB_ENV
      - name: Upload test results
        uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: results ${{ env.ARTIFACT_NAME }}
          path: |
            **/target/surefire-reports
            **/target/checkstyle-*
            **/*.hprof
          retention-days: 30
      - name: Annotate with test report
        uses: trinodb/github-actions/action-surefire-report@b63800bedfbc7ab1ff2e5fe7eaecf5ab82ce6a70
        if: failure()
        with:
          # this workflow should never fail, as it is not a quality gateway
          fail_if_no_tests: false
          skip_publishing: true
      - name: Should notify on failure
        id: should_notify
        if: always() && (steps.tests-bq.outcome == 'failure' || steps.tests-bq-smoke.outcome == 'failure')
        run: |
          # GITHUB_HEAD_REF is only populated for pull requests runs, for scheduled runs extract the name from GITHUB_REF
          branch_name="${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"
          echo "branch=$branch_name" >> "${GITHUB_OUTPUT}"
          if [[ $branch_name == "master" || $branch_name =~ ^release-.*-e$ ]]; then
            echo "result=true" >> "${GITHUB_OUTPUT}"
          else
            echo "result=false" >> "${GITHUB_OUTPUT}"
          fi
      - name: Slack notify failed tests to Connectors team
        if: always() && (steps.tests-bq.outcome == 'failure' || steps.tests-bq-smoke.outcome == 'failure') && steps.should_notify.outputs.result == 'true'
        uses: archive/github-actions-slack@433fef9978d3adae73168bfc9c5a7ce722780231 # v3.1.0
        with:
          slack-bot-user-oauth-access-token: ${{ secrets.SLACK_TOKEN }}
          slack-channel: C09HT9ETS73 # team-connectors-notify channel ID
          slack-text: |
            :boom: BigQuery tests failed on branch: ${{ steps.should_notify.outputs.branch }} (<https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|see CI job for details>) (:thumbsup: this message if you take care of this)
      - name: Launch Flake Tracker
        if: failure() && (steps.tests-bq.outcome == 'failure' || steps.tests-bq-smoke.outcome == 'failure') && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}

  # ------------------------------------------------------------------------
  # Only run Google Sheets tests on a schedule, labeled PRs, or workflow_dispatch
  # ------------------------------------------------------------------------
  google-sheets-tests:
    name: google-sheets-tests
    runs-on: [self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest]
    timeout-minutes: 30
    if: "(github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'google-sheets')) || (github.event_name == 'workflow_dispatch')"
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits, as the build result depends on `git describe` equivalent
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
      - uses: ./.github/actions/setup
      - name: Maven install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} -am -pl plugin/trino-google-sheets
      - name: Run Google Sheets tests
        id: tests
        run: |
          $MAVEN test ${MAVEN_TEST} -pl plugin/trino-google-sheets
      - name: Cloud Google Sheets Tests
        id: tests-google-sheets
        env:
          SHEETS_CREDENTIALS_KEY: ${{ secrets.SHEETS_CREDENTIALS_KEY }}
        run: |
          $MAVEN test ${MAVEN_TEST} -pl :trino-google-sheets -Pcloud-tests \
            -Dtesting.gsheets.credentials-key="${SHEETS_CREDENTIALS_KEY}"
      - name: Sanitize artifact name
        if: always()
        run: |
          # Generate a valid artifact name and make it available to next steps as
          # an environment variable ARTIFACT_NAME
          # ", :, <, >, |, *, ?, \, / are not allowed in artifact names, replace it with an underscore
          name=$(echo -n "plugin/trino-google-sheets" | sed -e 's/[":<>|\*\?\\\/]/_/g')
          echo "ARTIFACT_NAME=$name" >> $GITHUB_ENV
      - name: Upload test results
        uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: results ${{ env.ARTIFACT_NAME }}
          path: |
            **/target/surefire-reports
            **/target/checkstyle-*
            **/*.hprof
          retention-days: 30
      - name: Annotate with test report
        uses: trinodb/github-actions/action-surefire-report@b63800bedfbc7ab1ff2e5fe7eaecf5ab82ce6a70
        if: failure()
        with:
          # this workflow should never fail, as it is not a quality gateway
          fail_if_no_tests: false
          skip_publishing: true
      - name: Should notify on failure
        if: always() && steps.tests.outcome == 'failure'
        run: |
          # GITHUB_HEAD_REF is only populated for pull requests runs, for scheduled runs extract the name from GITHUB_REF
          branch_name="${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"
          echo "branch=$branch_name" >> "${GITHUB_OUTPUT}"
          if [[ $branch_name == "master" || $branch_name =~ ^release-.*-e$ ]]; then
            echo "result=true" >> "${GITHUB_OUTPUT}"
          else
            echo "result=false" >> "${GITHUB_OUTPUT}"
          fi
        id: should_notify
      - name: Slack notify failed tests to Connectors team
        if: always() && steps.tests.outcome == 'failure' && steps.should_notify.outputs.result == 'true'
        uses: archive/github-actions-slack@433fef9978d3adae73168bfc9c5a7ce722780231 # v3.1.0
        with:
          slack-bot-user-oauth-access-token: ${{ secrets.SLACK_TOKEN }}
          slack-channel: C09HT9ETS73 # team-connectors-notify channel ID
          slack-text: |
            :boom: Google Sheets tests failed on branch: ${{ steps.should_notify.outputs.branch }} (<https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|see CI job for details>) (:thumbsup: this message if you take care of this)
      - name: Launch Flake Tracker
        if: failure() && steps.tests.outcome == 'failure' && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  # ------------------------------------------------------------------------
  # Only run Iceberg BigLake tests on a schedule, labeled PRs, or workflow_dispatch
  # ------------------------------------------------------------------------
  iceberg-biglake-tests:
    name: iceberg-biglake-tests
    runs-on: [self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest]
    timeout-minutes: 30
    if: "(github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'iceberg:biglake')) || (github.event_name == 'workflow_dispatch')"
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits, as the build result depends on `git describe` equivalent
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
      - uses: ./.github/actions/setup
      - name: Maven install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} -am -pl plugin/trino-iceberg
      - name: Iceberg BigLake Tests
        id: tests-iceberg-biglake
        env:
          GCP_STORAGE_BUCKET: ${{ vars.GCP_STORAGE_BUCKET }}
          GCP_CREDENTIALS_KEY: ${{ secrets.GCP_CREDENTIALS_KEY }}
        run: |
          $MAVEN test ${MAVEN_TEST} -pl :trino-iceberg -Pbiglake-tests
      - name: Sanitize artifact name
        if: always()
        run: echo "ARTIFACT_NAME=plugin_trino-iceberg" >> $GITHUB_ENV
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: results ${{ env.ARTIFACT_NAME }}
          path: |
            **/target/surefire-reports
            **/target/checkstyle-*
            **/*.hprof
          retention-days: 30
      - name: Annotate with test report
        uses: trinodb/github-actions/action-surefire-report@b63800bedfbc7ab1ff2e5fe7eaecf5ab82ce6a70
        if: failure()
        with:
          # this workflow should never fail, as it is not a quality gateway
          fail_if_no_tests: false
          skip_publishing: true
      - name: Should notify on failure
        if: always() && steps.tests-iceberg-biglake.outcome == 'failure'
        run: |
          # GITHUB_HEAD_REF is only populated for pull requests runs, for scheduled runs extract the name from GITHUB_REF
          branch_name="${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"
          echo "branch=$branch_name" >> "${GITHUB_OUTPUT}"
          if [[ $branch_name == "master" || $branch_name =~ ^release-.*-e$ ]]; then
            echo "result=true" >> "${GITHUB_OUTPUT}"
          else
            echo "result=false" >> "${GITHUB_OUTPUT}"
          fi
        id: should_notify
      - name: Slack notify failed tests to Connectors team
        if: always() && steps.tests-iceberg-biglake.outcome == 'failure' && steps.should_notify.outputs.result == 'true'
        uses: archive/github-actions-slack@433fef9978d3adae73168bfc9c5a7ce722780231 # v3.1.0
        with:
          slack-bot-user-oauth-access-token: ${{ secrets.SLACK_TOKEN }}
          slack-channel: C09HT9ETS73 # team-connectors-notify channel ID
          slack-text: |
            :boom: Iceberg BigLake tests failed on branch: ${{ steps.should_notify.outputs.branch }} (<https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|see CI job for details>) (:thumbsup: this message if you take care of this)
      - name: Launch Flake Tracker
        if: failure() && steps.tests-iceberg-biglake.outcome == 'failure' && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  # ------------------------------------------------------------------------
  # Only run redshift tests on a schedule, labeled PRs, or workflow_dispatch
  # ------------------------------------------------------------------------
  redshift-cloud-tests:
    name: redshift-cloud-tests
    runs-on: [self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest]
    strategy:
      fail-fast: false
      matrix:
        modules:
          - plugin/trino-redshift
    timeout-minutes: 90
    if: "(github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'redshift')) || (github.event_name == 'workflow_dispatch')"
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits, as the build result depends on `git describe` equivalent
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
      - uses: ./.github/actions/setup
      - name: Maven install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} -am -pl ${{ matrix.modules }}
      - name: Run Redshift Cloud tests
        id: tests
        if: contains(matrix.modules, 'trino-redshift')
        env:
          AWS_REGION: ${{ vars.REDSHIFT_AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ vars.REDSHIFT_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.REDSHIFT_AWS_SECRET_ACCESS_KEY }}
          ASSUME_IAM_ROLE_AWS_ACCESS_KEY_ID: ${{ vars.REDSHIFT_ASSUME_IAM_ROLE_AWS_ACCESS_KEY_ID }}
          ASSUME_IAM_ROLE_AWS_SECRET_ACCESS_KEY: ${{ secrets.REDSHIFT_ASSUME_IAM_ROLE_AWS_SECRET_ACCESS_KEY }}
          REDSHIFT_SUBNET_GROUP_NAME: ${{ vars.REDSHIFT_SUBNET_GROUP_NAME }}
          REDSHIFT_IAM_ROLES: ${{ vars.REDSHIFT_IAM_ROLES }}
          REDSHIFT_VPC_SECURITY_GROUP_IDS: ${{ vars.REDSHIFT_VPC_SECURITY_GROUP_IDS }}
          REDSHIFT_S3_TPCH_TABLES_ROOT: ${{ vars.REDSHIFT_S3_TPCH_TABLES_ROOT }}
          REDSHIFT_S3_EXTERNAL_TABLES_ROOT: ${{ vars.REDSHIFT_S3_EXTERNAL_TABLES_ROOT }}
          REDSHIFT_S3_UNLOAD_ROOT: ${{ vars.REDSHIFT_S3_UNLOAD_ROOT }}
        run: |
          source .github/bin/redshift/setup-aws-redshift.sh

          $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} -P cloud-tests \
            -Dtest.redshift.jdbc.user="${REDSHIFT_USER}" \
            -Dtest.redshift.jdbc.password="${REDSHIFT_PASSWORD}" \
            -Dtest.redshift.jdbc.endpoint="${REDSHIFT_ENDPOINT}:${REDSHIFT_PORT}/" \
            -Dtest.redshift.s3.tpch.tables.root="${REDSHIFT_S3_TPCH_TABLES_ROOT}" \
            -Dtest.redshift.s3.external.tables.root="${REDSHIFT_S3_EXTERNAL_TABLES_ROOT}" \
            -Dtest.redshift.s3.unload.root="${REDSHIFT_S3_UNLOAD_ROOT}" \
            -Dtest.redshift.iam.role="${REDSHIFT_IAM_ROLES}" \
            -Dtest.redshift.aws.region="${AWS_REGION}" \
            -Dtest.redshift.aws.access-key="${AWS_ACCESS_KEY_ID}" \
            -Dtest.redshift.aws.secret-key="${AWS_SECRET_ACCESS_KEY}" \
            -Dtest.redshift.assume.iam.role.aws.access-key="${ASSUME_IAM_ROLE_AWS_ACCESS_KEY_ID}" \
            -Dtest.redshift.assume.iam.role.aws.secret-key="${ASSUME_IAM_ROLE_AWS_SECRET_ACCESS_KEY}"
      - name: Cleanup ephemeral Redshift Cluster
        env:
          AWS_REGION: ${{ vars.REDSHIFT_AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ vars.REDSHIFT_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.REDSHIFT_AWS_SECRET_ACCESS_KEY }}
        # Cancelled workflows may have left the ephemeral cluster running
        if: always()
        run: .github/bin/redshift/delete-aws-redshift.sh
      - name: Sanitize artifact name
        if: always()
        run: |
          # Generate a valid artifact name and make it available to next steps as
          # an environment variable ARTIFACT_NAME
          # ", :, <, >, |, *, ?, \, / are not allowed in artifact names, replace it with an underscore
          name=$(echo -n "${{ matrix.modules }}" | sed -e 's/[":<>|\*\?\\\/]/_/g')
          echo "ARTIFACT_NAME=$name" >> $GITHUB_ENV
      - name: Upload test results
        uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: results ${{ env.ARTIFACT_NAME }}
          path: |
            **/target/surefire-reports
            **/target/checkstyle-*
            **/*.hprof
          retention-days: 30
      - name: Annotate with test report
        uses: trinodb/github-actions/action-surefire-report@b63800bedfbc7ab1ff2e5fe7eaecf5ab82ce6a70
        if: failure()
        with:
          # this workflow should never fail, as it is not a quality gateway
          fail_if_no_tests: false
          skip_publishing: true
      - name: Should notify on failure
        if: always() && steps.tests.outcome == 'failure'
        run: |
          # GITHUB_HEAD_REF is only populated for pull requests runs, for scheduled runs extract the name from GITHUB_REF
          branch_name="${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"
          echo "branch=$branch_name" >> "${GITHUB_OUTPUT}"
          if [[ $branch_name == "master" || $branch_name =~ ^release-.*-e$ ]]; then
            echo "result=true" >> "${GITHUB_OUTPUT}"
          else
            echo "result=false" >> "${GITHUB_OUTPUT}"
          fi
        id: should_notify
      - name: Slack notify failed tests to Connectors team
        if: always() && steps.tests.outcome == 'failure' && steps.should_notify.outputs.result == 'true'
        uses: archive/github-actions-slack@433fef9978d3adae73168bfc9c5a7ce722780231 # v3.1.0
        with:
          slack-bot-user-oauth-access-token: ${{ secrets.SLACK_TOKEN }}
          slack-channel: C09HT9ETS73 # galaxy-data-management-notify channel ID
          slack-text: |
            :boom: Redshift cloud tests failed on branch: ${{ steps.should_notify.outputs.branch }} (<https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|see CI job for details>) (:thumbsup: this message if you take care of this)
      - name: Launch Flake Tracker
        if: failure() && steps.tests.outcome == 'failure' && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  # ------------------------------------------------------------------------
  # Only run redshift tests on a schedule, labeled PRs, or workflow_dispatch
  # ------------------------------------------------------------------------
  redshift-fte-tests:
    name: redshift-fte-tests
    runs-on: [self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest]
    strategy:
      fail-fast: false
      matrix:
        modules:
          - plugin/trino-redshift
    timeout-minutes: 90
    if: "(github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'redshift')) || (github.event_name == 'workflow_dispatch')"
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits, as the build result depends on `git describe` equivalent
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
      - uses: ./.github/actions/setup
      - name: Maven install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} -am -pl ${{ matrix.modules }}
      - name: Run Redshift FTE tests
        id: tests
        if: contains(matrix.modules, 'trino-redshift')
        env:
          AWS_REGION: ${{ vars.REDSHIFT_AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ vars.REDSHIFT_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.REDSHIFT_AWS_SECRET_ACCESS_KEY }}
          REDSHIFT_SUBNET_GROUP_NAME: ${{ vars.REDSHIFT_SUBNET_GROUP_NAME }}
          REDSHIFT_IAM_ROLES: ${{ vars.REDSHIFT_IAM_ROLES }}
          REDSHIFT_VPC_SECURITY_GROUP_IDS: ${{ vars.REDSHIFT_VPC_SECURITY_GROUP_IDS }}
          REDSHIFT_S3_TPCH_TABLES_ROOT: ${{ vars.REDSHIFT_S3_TPCH_TABLES_ROOT }}
          REDSHIFT_S3_UNLOAD_ROOT: ${{ vars.REDSHIFT_S3_UNLOAD_ROOT }}
        run: |
          source .github/bin/redshift/setup-aws-redshift.sh

          $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} -P fte-tests \
            -Dtest.redshift.jdbc.user="${REDSHIFT_USER}" \
            -Dtest.redshift.jdbc.password="${REDSHIFT_PASSWORD}" \
            -Dtest.redshift.jdbc.endpoint="${REDSHIFT_ENDPOINT}:${REDSHIFT_PORT}/" \
            -Dtest.redshift.s3.tpch.tables.root="${REDSHIFT_S3_TPCH_TABLES_ROOT}" \
            -Dtest.redshift.s3.unload.root="${REDSHIFT_S3_UNLOAD_ROOT}" \
            -Dtest.redshift.iam.role="${REDSHIFT_IAM_ROLES}" \
            -Dtest.redshift.aws.region="${AWS_REGION}" \
            -Dtest.redshift.aws.access-key="${AWS_ACCESS_KEY_ID}" \
            -Dtest.redshift.aws.secret-key="${AWS_SECRET_ACCESS_KEY}"
      - name: Cleanup ephemeral Redshift Cluster
        env:
          AWS_REGION: ${{ vars.REDSHIFT_AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ vars.REDSHIFT_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.REDSHIFT_AWS_SECRET_ACCESS_KEY }}
        # Cancelled workflows may have left the ephemeral cluster running
        if: always()
        run: .github/bin/redshift/delete-aws-redshift.sh
      - name: Sanitize artifact name
        if: always()
        run: |
          # Generate a valid artifact name and make it available to next steps as
          # an environment variable ARTIFACT_NAME
          # ", :, <, >, |, *, ?, \, / are not allowed in artifact names, replace it with an underscore
          name=$(echo -n "${{ matrix.modules }}" | sed -e 's/[":<>|\*\?\\\/]/_/g')
          echo "ARTIFACT_NAME=$name" >> $GITHUB_ENV
      - name: Upload test results
        uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: results ${{ env.ARTIFACT_NAME }}
          path: |
            **/target/surefire-reports
            **/target/checkstyle-*
            **/*.hprof
          retention-days: 30
      - name: Annotate with test report
        uses: trinodb/github-actions/action-surefire-report@b63800bedfbc7ab1ff2e5fe7eaecf5ab82ce6a70
        if: failure()
        with:
          # this workflow should never fail, as it is not a quality gateway
          fail_if_no_tests: false
          skip_publishing: true
      - name: Should notify on failure
        if: always() && steps.tests.outcome == 'failure'
        run: |
          # GITHUB_HEAD_REF is only populated for pull requests runs, for scheduled runs extract the name from GITHUB_REF
          branch_name="${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"
          echo "branch=$branch_name" >> "${GITHUB_OUTPUT}"
          if [[ $branch_name == "master" || $branch_name =~ ^release-.*-e$ ]]; then
            echo "result=true" >> "${GITHUB_OUTPUT}"
          else
            echo "result=false" >> "${GITHUB_OUTPUT}"
          fi
        id: should_notify
      - name: Slack notify failed tests to Connectors team
        if: always() && steps.tests.outcome == 'failure' && steps.should_notify.outputs.result == 'true'
        uses: archive/github-actions-slack@433fef9978d3adae73168bfc9c5a7ce722780231 # v3.1.0
        with:
          slack-bot-user-oauth-access-token: ${{ secrets.SLACK_TOKEN }}
          slack-channel: C09HT9ETS73 # galaxy-data-management-notify channel ID
          slack-text: |
            :boom: Redshift FTE tests failed on branch: ${{ steps.should_notify.outputs.branch }} (<https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|see CI job for details>) (:thumbsup: this message if you take care of this)
      - name: Launch Flake Tracker
        if: failure() && steps.tests.outcome == 'failure' && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  # ----------------------------------------------------------------------------
  # Only run salesforce tests on a schedule, labeled PRs, or workflow_dispatch to avoid salesforce rate limits
  # ----------------------------------------------------------------------------
  salesforce-tests:
    name: salesforce-tests (${{ matrix.modules }})
    runs-on: [self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest]
    strategy:
      fail-fast: false
      matrix:
        modules:
          - plugin/starburst-salesforce
    timeout-minutes: 30
    if: "(github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'salesforce')) || (github.event_name == 'workflow_dispatch')"
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits, as the build result depends on `git describe` equivalent
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
      - uses: ./.github/actions/setup
      - name: Maven install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} -am -pl ${{ matrix.modules }}
      - name: Run Salesforce tests
        id: tests
        if: contains(matrix.modules, 'starburst-salesforce')
        env:
          SALESFORCE_BASIC_AUTH_USER_USERNAME: ${{ vars.SALESFORCE_BASIC_AUTH_USER_USERNAME }}
          SALESFORCE_BASIC_AUTH_USER_PASSWORD: ${{ secrets.SALESFORCE_BASIC_AUTH_USER_PASSWORD }}
          SALESFORCE_BASIC_AUTH_USER_SECURITY_TOKEN: ${{ secrets.SALESFORCE_BASIC_AUTH_USER_SECURITY_TOKEN }}
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} \
            -Dsalesforce.test.basic.auth.user="${SALESFORCE_BASIC_AUTH_USER_USERNAME}" \
            -Dsalesforce.test.basic.auth.password="${SALESFORCE_BASIC_AUTH_USER_PASSWORD}" \
            -Dsalesforce.test.basic.auth.security-token="${SALESFORCE_BASIC_AUTH_USER_SECURITY_TOKEN}"
      - name: Sanitize artifact name
        if: always()
        run: |
          # Generate a valid artifact name and make it available to next steps as
          # an environment variable ARTIFACT_NAME
          # ", :, <, >, |, *, ?, \, / are not allowed in artifact names, replace it with an underscore
          name=$(echo -n "${{ matrix.modules }}" | sed -e 's/[":<>|\*\?\\\/]/_/g')
          echo "ARTIFACT_NAME=$name" >> $GITHUB_ENV
      - name: Upload test results
        uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: results ${{ env.ARTIFACT_NAME }}
          path: |
            **/target/surefire-reports
            **/target/checkstyle-*
            **/*.hprof
          retention-days: 30
      - name: Annotate with test report
        uses: trinodb/github-actions/action-surefire-report@b63800bedfbc7ab1ff2e5fe7eaecf5ab82ce6a70
        if: failure()
        with:
          # this workflow should never fail, as it is not a quality gateway
          fail_if_no_tests: false
          skip_publishing: true
      - name: Should notify on failure
        if: always() && steps.tests.outcome == 'failure'
        run: |
          # GITHUB_HEAD_REF is only populated for pull requests runs, for scheduled runs extract the name from GITHUB_REF
          branch_name="${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"
          echo "branch=$branch_name" >> "${GITHUB_OUTPUT}"
          if [[ $branch_name == "master" || $branch_name =~ ^release-.*-e$ ]]; then
            echo "result=true" >> "${GITHUB_OUTPUT}"
          else
            echo "result=false" >> "${GITHUB_OUTPUT}"
          fi
        id: should_notify
      - name: Slack notify failed tests to Connectors team
        if: always() && steps.tests.outcome == 'failure' && steps.should_notify.outputs.result == 'true'
        uses: archive/github-actions-slack@433fef9978d3adae73168bfc9c5a7ce722780231 # v3.1.0
        with:
          slack-bot-user-oauth-access-token: ${{ secrets.SLACK_TOKEN }}
          slack-channel: C09HT9ETS73 # galaxy-data-management-notify channel ID
          slack-text: |
            :boom: Salesforce connector tests failed on branch: ${{ steps.should_notify.outputs.branch }} (<https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|see CI job for details>) (:thumbsup: this message if you take care of this)
      - name: Launch Flake Tracker
        if: failure() && steps.tests.outcome == 'failure' && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  # -----------------------------------------------------------
  # Synapse tests - start a SQL pool if needed
  # nightly tests run on any scheduled run or labeled PR, while
  # weekly and PT only run on labeled PRs or the specific weekly schedule
  # -----------------------------------------------------------
  synapse-prepare:
    name: synapse-prepare
    permissions:
      id-token: write
      contents: read
    runs-on: [ self-hosted, gha-fleet-ec2-2cpu-1gb-arm64, latest ]
    if: |
      github.event_name == 'schedule' ||
      (github.event_name == 'pull_request' && (
        contains(github.event.pull_request.labels.*.name, 'synapse-smoke') ||
        contains(github.event.pull_request.labels.*.name, 'synapse')
      ))
    outputs:
      matrix: ${{ steps.build-synapse-matrix.outputs.MATRIX }}
      matrix-with-pt: ${{ steps.build-synapse-matrix.outputs.PT_MATRIX }}
      sql-pool-name: ${{ steps.generate-pool-name.outputs.SYNAPSE_SQL_POOL }}
    steps:
      - uses: actions/checkout@v6
      - name: Build matrix
        id: build-synapse-matrix
        env:
          GH_REPOSITORY: ${{ github.repository }}
          GH_EVENT_NAME: ${{ github.event_name }}
          GH_JOB_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}/job/${{ job.check_run_id }}
          IS_WEEKLY_SCHEDULE: ${{ github.event_name == 'schedule' && github.event.schedule == '0 0 * * 6' }}
          HAS_SYNAPSE_LABEL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'synapse') }}
        run: |
          pool_tags="$(jq -nc \
            --arg repo "${GH_REPOSITORY}" \
            --arg event "${GH_EVENT_NAME}" \
            --arg job_url "${GH_JOB_URL}" \
          '{
            "repository": $repo,
            "event": $event,
            "job_url": $job_url,
            "test_type": "smoke"
          }')"
          matrix=$(jq -nc '{"include": [{"synapse-test": "smoke"}]}')
          pt_matrix="$matrix"
          if [[ "$IS_WEEKLY_SCHEDULE" == "true" || "$HAS_SYNAPSE_LABEL" == 'true' ]]; then
            pool_tags=$(echo "$pool_tags" | jq -c '.test_type = "full"')
            matrix=$(echo "$matrix" | jq -c '.include += [
              { "synapse-test": "full", "profile": "fte-tests" },
              { "synapse-test": "full", "profile": "long-running-tests" },
              { "synapse-test": "full", "profile": "type-mapping-tests" }
            ]')
            pt_matrix=$(echo "$matrix" | jq -c '.include += [{"synapse-test": "pt"}]')
          fi
          echo "pool_tags=$pool_tags" >> $GITHUB_OUTPUT
          echo "MATRIX=$matrix" >> $GITHUB_OUTPUT
          echo "PT_MATRIX=$pt_matrix" >> $GITHUB_OUTPUT
      - name: Azure Login
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          client-id: ${{ secrets.SYNAPSE_CLIENT_ID }}
          tenant-id: ${{ secrets.SYNAPSE_TENANT_ID }}
          subscription-id: ${{ secrets.SYNAPSE_SUBSCRIPTION_ID }}
      # Output pool name before starting the pool so the pool can be stopped even if this job is canceled before the pool is fully started
      - name: Generate Pool Name
        id: generate-pool-name
        run: |
          echo "SYNAPSE_SQL_POOL=POOL_$(openssl rand -hex 4)" | tee -a "$GITHUB_OUTPUT"
      - name: Start Pool
        id: start-pool
        uses: azure/cli@9eb25b8360668fb0ecbafa808d40e2197b2f5f52 # v3.0.0
        env:
          SYNAPSE_RESOURCE_GROUP: ${{ vars.SYNAPSE_RESOURCE_GROUP }}
          SYNAPSE_WORKSPACE: ${{ vars.SYNAPSE_WORKSPACE }}
          SYNAPSE_SQL_POOL: ${{ steps.generate-pool-name.outputs.SYNAPSE_SQL_POOL }}
          SYNAPSE_POOL_TAGS: ${{ steps.build-synapse-matrix.outputs.pool_tags }}
        with:
          inlineScript: |
            "$GITHUB_WORKSPACE"/.github/bin/synapse/start-sql-pool.sh
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"
  synapse-tests:
    name: synapse-tests (${{ matrix.synapse-test }}${{ matrix.profile != '' && format(', {0}', matrix.profile) || '' }})
    runs-on: [ self-hosted, gha-fleet-ec2-4cpu-16gb-arm64, latest ]
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.build-pt.result == 'success' && needs.synapse-prepare.outputs.matrix-with-pt || needs.synapse-prepare.outputs.matrix) }}
    timeout-minutes: 90
    needs: [ build-pt, synapse-prepare ]
    # Skip build-pt success check so nightly and weekly can run regardless
    # Matrix excludes PT cell if build-pt was skipped
    if: always() && needs.synapse-prepare.result == 'success'
    env:
      REPORT_TEST_SESSION_NAME: ${{ matrix.synapse-test == 'pt' && 'pt' || 'modules' }}
      REPORT_SERVICE_NAME: ${{ matrix.synapse-test == 'pt' && 'suite-synapse-default' || 'starburst-synapse' }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits, as the build result depends on `git describe` equivalent
      - uses: ./.github/actions/setup
      - name: Set up AWS code-artifacts and ECR logins
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
          login-docker: ${{ matrix.synapse-test == 'pt' }}
      ### PRODUCT TEST STEPS
      - name: Product tests artifact
        uses: actions/download-artifact@v8
        if: matrix.synapse-test == 'pt'
        with:
          name: product tests and server tarball
      - name: Fix product tests artifact permissions
        if: matrix.synapse-test == 'pt'
        run: |
          find . -type f -name \*-executable.jar -exec chmod 0777 {} \;
      - name: Enable PT impact analysis
        if: |
          matrix.synapse-test == 'pt' &&
          needs.build-pt.outputs.product-tests-changed == 'false' &&
          github.event_name == 'pull_request' &&
          !contains(github.event.pull_request.labels.*.name, 'tests:all') &&
          !contains(github.event.pull_request.labels.*.name, 'tests:all-product')
        run: echo "PTL_OPTS=--impacted-features impacted-features.log" >> $GITHUB_ENV
      ### PLUGIN TEST STEPS
      - name: Maven install
        if: matrix.synapse-test != 'pt'
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} -am -pl 'plugin/starburst-synapse'
      ### ANY TEST STEPS
      - name: Run Synapse tests
        id: tests
        env:
          SYNAPSE_ENDPOINT: ${{ vars.SYNAPSE_ENDPOINT }}
          SYNAPSE_USER: ${{ vars.SYNAPSE_USER }}
          SYNAPSE_PASSWORD: ${{ env.SEP_CI_SEP_SYNAPSE_PASSWORD }}
          SYNAPSE_DATABASE: ${{ needs.synapse-prepare.outputs.sql-pool-name }}
        if: |
          env.SYNAPSE_ENDPOINT != '' && env.SYNAPSE_USER != '' && env.SYNAPSE_PASSWORD != ''
        run: |
          if [[ "${{ matrix.synapse-test }}" == "pt" ]]; then
            testing/trino-product-tests-launcher/target/trino-product-tests-launcher-*-executable.jar suite run \
              --suite suite-synapse \
              --config config-default \
              ${PTL_OPTS:-} \
              --bind=off --logs-dir logs/ --timeout 80m
          else
            export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
            $MAVEN test ${MAVEN_TEST} -pl 'plugin/starburst-synapse' ${{ matrix.profile != '' && format('-P {0}', matrix.profile) || '' }} \
              -Dtest.synapse.jdbc.endpoint="${SYNAPSE_ENDPOINT}" \
              -Dtest.synapse.jdbc.user="${SYNAPSE_USER}" \
              -Dtest.synapse.jdbc.password="${SYNAPSE_PASSWORD}" \
              -Dtest.synapse.jdbc.sqlpool="${SYNAPSE_DATABASE}"
          fi
      - name: Sanitize artifact name
        if: always()
        run: |
          # Generate a valid artifact name and make it available to next steps as
          # an environment variable ARTIFACT_NAME
          # ", :, <, >, |, *, ?, \, / are not allowed in artifact names, replace it with an underscore
          name=$(echo -n "synapse-tests_${{ matrix.synapse-test }}${{ matrix.profile != '' && format('_{0}', matrix.profile) || '' }}" | sed -e 's/[":<>|\*\?\\\/]/_/g')
          echo "ARTIFACT_NAME=$name" >> $GITHUB_ENV
      - name: Upload test results
        uses: ./.github/actions/process-test-results
        if: always()
        with:
          artifact-name: ${{ env.ARTIFACT_NAME }}
          has-failed-tests: ${{ steps.tests.outcome == 'failure' }}
          upload-heap-dump: ${{ env.SECRETS_PRESENT == '' && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
      - name: Upload test results to Datadog
        uses: ./.github/actions/upload-test-results-datadog
        if: always()
        with:
          datadog-api-key: ${{ secrets.DATADOG_API_KEY }}
      - name: Should notify on failure
        if: always() && steps.tests.outcome == 'failure'
        run: |
          # GITHUB_HEAD_REF is only populated for pull requests runs, for scheduled runs extract the name from GITHUB_REF
          branch_name="${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"
          echo "branch=$branch_name" >> "${GITHUB_OUTPUT}"
          if [[ $branch_name == "master" || $branch_name =~ ^release-.*-e$ ]]; then
            echo "result=true" >> "${GITHUB_OUTPUT}"
          else
            echo "result=false" >> "${GITHUB_OUTPUT}"
          fi
        id: should_notify
      - name: Slack notify failed tests to Connectors team
        if: always() && steps.tests.outcome == 'failure' && steps.should_notify.outputs.result == 'true'
        uses: archive/github-actions-slack@433fef9978d3adae73168bfc9c5a7ce722780231 # v3.1.0
        with:
          slack-bot-user-oauth-access-token: ${{ secrets.SLACK_TOKEN }}
          slack-channel: C09HT9ETS73 # galaxy-data-management-notify channel ID
          slack-text: |
            :boom: Synapse ${{ matrix.synapse-test }} tests failed on branch: ${{ steps.should_notify.outputs.branch }} (<https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|see CI job for details>) (:thumbsup: this message if you will take care of this)
      - name: Launch Flake Tracker
        if: failure() && steps.tests.outcome == 'failure' && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"
  # --------------------------------------------------------------
  # After synapse tests finish, tear down SQL pool
  # --------------------------------------------------------------
  synapse-stop-pool:
    name: synapse-stop-pool
    permissions:
      id-token: write
      contents: read
    runs-on: [ self-hosted, gha-fleet-ec2-2cpu-1gb-arm64, latest ]
    needs: [ synapse-prepare, synapse-tests ]
    # Ignore status of tests, but only stop the pool if there's (potentially) one to stop.
    # We don't check if synapse-prepare succeeded in case it was canceled while starting the pool
    if: always() && needs.synapse-prepare.outputs.sql-pool-name != ''
    steps:
      - uses: actions/checkout@v6
      - name: Azure Login
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          client-id: ${{ secrets.SYNAPSE_CLIENT_ID }}
          tenant-id: ${{ secrets.SYNAPSE_TENANT_ID }}
          subscription-id: ${{ secrets.SYNAPSE_SUBSCRIPTION_ID }}
      - name: Stop Pool
        uses: azure/cli@9eb25b8360668fb0ecbafa808d40e2197b2f5f52 # v3.0.0
        env:
          SYNAPSE_SQL_POOL: ${{ needs.synapse-prepare.outputs.sql-pool-name }}
          SYNAPSE_RESOURCE_GROUP: ${{ vars.SYNAPSE_RESOURCE_GROUP }}
          SYNAPSE_WORKSPACE: ${{ vars.SYNAPSE_WORKSPACE }}
        with:
          inlineScript: "$GITHUB_WORKSPACE/.github/bin/synapse/stop-sql-pool.sh"
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"
  # --------------------------------------------------------------
  # Weekly run all snowflake tests, labeled PRs to limit CI costs
  # --------------------------------------------------------------
  snowflake-weekly-tests:
    name: snowflake-weekly-tests
    runs-on: [ self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest ]
    strategy:
      fail-fast: false
      matrix:
        modules: [ plugin/starburst-snowflake ]
        profile: [ default, smoke-tests, jdbc-integration-tests, parallel-integration-tests, fte-tests ]
        cloud: [ amazon-web-service, azure ]
        # Additional job filtering to include Azure only for 'default' and 'smoke-tests' profile
        exclude:
          - profile: jdbc-integration-tests
            cloud: azure
          - profile: parallel-integration-tests
            cloud: azure
          - profile: fte-tests
            cloud: azure

    timeout-minutes: 90
    if: "(github.event_name == 'schedule' && github.event.schedule == '0 0 * * 6') || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'snowflake'))"
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits, as the build result depends on `git describe` equivalent
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
      - uses: ./.github/actions/setup
      - name: Maven install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} -am -pl ${{ matrix.modules }}
      - name: Run Snowflake AWS weekly tests
        id: tests-weekly-snowflake-aws
        if: >-
          contains(matrix.modules, 'starburst-snowflake') &&
          contains(matrix.cloud, 'amazon-web-service') &&
          (env.SNOWFLAKE_ACCOUNT_NAME != '' || env.SNOWFLAKE_PASSWORD != '' || env.SNOWFLAKE_PRIVATE_KEY != '')
        env:
          SNOWFLAKE_USER: ${{ vars.SNOWFLAKE_USER }}
          SNOWFLAKE_ACCOUNT_NAME: ${{ vars.SNOWFLAKE_ACCOUNT_NAME }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_PRIVATE_KEY: ${{ secrets.SNOWFLAKE_PRIVATE_KEY }}
          SNOWFLAKE_PRIVATE_KEY_2: ${{ secrets.SNOWFLAKE_PRIVATE_KEY_2 }}
          SNOWFLAKE_PRIVATE_KEY_2_PASSPHRASE: ${{ secrets.SNOWFLAKE_PRIVATE_KEY_2_PASSPHRASE }}
          SNOWFLAKE_ROLE: ${{ vars.SNOWFLAKE_ROLE }}
          SNOWFLAKE_CONNECTION_PARAMS: ${{ vars.SNOWFLAKE_CONNECTION_PARAMETERS }}
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} ${{ matrix.profile != '' && format('-P {0}', matrix.profile) || '' }} \
          -Dsnowflake.test.server.url=jdbc:snowflake://"${SNOWFLAKE_ACCOUNT_NAME}".snowflakecomputing.com/?${SNOWFLAKE_CONNECTION_PARAMS} \
          -Dsnowflake.test.server.user="${SNOWFLAKE_USER}" \
          -Dsnowflake.test.server.password="${SNOWFLAKE_PASSWORD}" \
          -Dsnowflake.test.server.private-key="${SNOWFLAKE_PRIVATE_KEY}" \
          -Dsnowflake.test.server.private-key-2="${SNOWFLAKE_PRIVATE_KEY_2}" \
          -Dsnowflake.test.server.private-key-2-passphrase="${SNOWFLAKE_PRIVATE_KEY_2_PASSPHRASE}" \
          -Dsnowflake.test.server.role="${SNOWFLAKE_ROLE}" \
          -Dsnowflake.test.warehouse=TRINO_PLUGINS_TEST_WH \
      - name: Run Snowflake Azure weekly tests
        id: tests-weekly-snowflake-azure
        if: >-
          contains(matrix.modules, 'starburst-snowflake') &&
          contains(matrix.cloud, 'azure') &&
          (env.SNOWFLAKE_AZURE_ACCOUNT_NAME != '' || env.SNOWFLAKE_AZURE_PASSWORD != '' || env.SNOWFLAKE_AZURE_PRIVATE_KEY != '' || env.SNOWFLAKE_AZURE_REGION != '')
        env:
          SNOWFLAKE_AZURE_ACCOUNT_NAME: ${{ vars.SNOWFLAKE_AZURE_ACCOUNT_NAME }}
          SNOWFLAKE_AZURE_PASSWORD: ${{ secrets.SNOWFLAKE_AZURE_PASSWORD }}
          SNOWFLAKE_AZURE_PRIVATE_KEY: ${{ secrets.SNOWFLAKE_AZURE_PRIVATE_KEY }}
          SNOWFLAKE_AZURE_PRIVATE_KEY_2: ${{ secrets.SNOWFLAKE_AZURE_PRIVATE_KEY_2 }}
          SNOWFLAKE_AZURE_PRIVATE_KEY_2_PASSPHRASE: ${{ secrets.SNOWFLAKE_AZURE_PRIVATE_KEY_2_PASSPHRASE }}
          SNOWFLAKE_AZURE_REGION: ${{ vars.SNOWFLAKE_AZURE_REGION }}
          SNOWFLAKE_CONNECTION_PARAMS: ${{ vars.SNOWFLAKE_CONNECTION_PARAMETERS }}
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} ${{ matrix.profile != '' && format('-P {0}', matrix.profile) || '' }} \
          -Dsnowflake.test.server.url=jdbc:snowflake://"${SNOWFLAKE_AZURE_ACCOUNT_NAME}"."${SNOWFLAKE_AZURE_REGION}".azure.snowflakecomputing.com/?${SNOWFLAKE_CONNECTION_PARAMS} \
          -Dsnowflake.test.server.user=test_user \
          -Dsnowflake.test.server.password="${SNOWFLAKE_AZURE_PASSWORD}" \
          -Dsnowflake.test.server.private-key="${SNOWFLAKE_AZURE_PRIVATE_KEY}" \
          -Dsnowflake.test.server.private-key-2="${SNOWFLAKE_AZURE_PRIVATE_KEY_2}" \
          -Dsnowflake.test.server.private-key-2-passphrase="${SNOWFLAKE_AZURE_PRIVATE_KEY_2_PASSPHRASE}" \
          -Dsnowflake.test.server.role=test_role \
          -Dsnowflake.test.warehouse=TRINO_PLUGINS_TEST_WH \
      - name: Sanitize artifact name
        if: always()
        run: |
          # Generate a valid artifact name and make it available to next steps as
          # an environment variable ARTIFACT_NAME
          # ", :, <, >, |, *, ?, \, / are not allowed in artifact names, replace it with an underscore
          name=$(echo -n "${{ matrix.modules }}" | sed -e 's/[":<>|\*\?\\\/]/_/g')
          echo "ARTIFACT_NAME=$name" >> $GITHUB_ENV
      - name: Upload test results
        uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: results ${{ env.ARTIFACT_NAME }}
          path: |
            **/target/surefire-reports
            **/target/checkstyle-*
            **/*.hprof
          retention-days: 30
      - name: Annotate with test report
        uses: trinodb/github-actions/action-surefire-report@b63800bedfbc7ab1ff2e5fe7eaecf5ab82ce6a70
        if: failure()
        with:
          # this workflow should never fail, as it is not a quality gateway
          fail_if_no_tests: false
          skip_publishing: true
      - name: Should notify on failure
        if: always() && (steps.tests-weekly-snowflake-aws.outcome == 'failure' || steps.tests-weekly-snowflake-azure.outcome == 'failure')
        run: |
          # GITHUB_HEAD_REF is only populated for pull requests runs, for scheduled runs extract the name from GITHUB_REF
          branch_name="${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"
          echo "branch=$branch_name" >> "${GITHUB_OUTPUT}"
          if [[ $branch_name == "master" || $branch_name =~ ^release-.*-e$ ]]; then
            echo "result=true" >> "${GITHUB_OUTPUT}"
          else
            echo "result=false" >> "${GITHUB_OUTPUT}"
          fi
        id: should_notify
      - name: Slack notify failed tests to Connectors team
        if: always() && (steps.tests-weekly-snowflake-aws.outcome == 'failure' || steps.tests-weekly-snowflake-azure.outcome == 'failure') && steps.should_notify.outputs.result == 'true'
        uses: archive/github-actions-slack@433fef9978d3adae73168bfc9c5a7ce722780231 # v3.1.0
        with:
          slack-bot-user-oauth-access-token: ${{ secrets.SLACK_TOKEN }}
          slack-channel: C09HT9ETS73 # galaxy-data-management-notify channel ID
          slack-text: |
            :boom: Snowflake weekly tests failed on branch: ${{ steps.should_notify.outputs.branch }} (<https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|see CI job for details>) (:thumbsup: this message if you take care of this)
      - name: Launch Flake Tracker
        if: failure() && (steps.tests-weekly-snowflake-aws.outcome == 'failure' || steps.tests-weekly-snowflake-azure.outcome == 'failure') && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"
  # --------------------------------------------------------------------------------------
  # Only run smoke-test profile snowflake tests nightly, labeled PRs to limit CI costs.
  # The release-* branches are excluded
  # --------------------------------------------------------------------------------------
  snowflake-nightly-tests:
    name: snowflake-nightly-tests
    runs-on: [ self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest ]
    strategy:
      fail-fast: false
      matrix:
        modules: [ plugin/starburst-snowflake ]
        profile: [ smoke-tests ]
        cloud: [ amazon-web-service ]
    timeout-minutes: 30
    if: "(github.event_name == 'schedule' && github.event.schedule == '0 0 * * *' && !startsWith(github.ref, 'refs/heads/release-')) || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'snowflake'))"
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits, as the build result depends on `git describe` equivalent
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
      - uses: ./.github/actions/setup
      - name: Maven install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} -am -pl ${{ matrix.modules }}
      - name: Run Snowflake AWS nightly tests
        id: tests-nightly-snowflake-aws
        if: >-
          contains(matrix.modules, 'starburst-snowflake') &&
          contains(matrix.cloud, 'amazon-web-service') &&
          (env.SNOWFLAKE_ACCOUNT_NAME != '' || env.SNOWFLAKE_PASSWORD != '' || env.SNOWFLAKE_PRIVATE_KEY != '')
        env:
          SNOWFLAKE_USER: ${{ vars.SNOWFLAKE_USER }}
          SNOWFLAKE_ACCOUNT_NAME: ${{ vars.SNOWFLAKE_ACCOUNT_NAME }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_PRIVATE_KEY: ${{ secrets.SNOWFLAKE_PRIVATE_KEY }}
          SNOWFLAKE_PRIVATE_KEY_2: ${{ secrets.SNOWFLAKE_PRIVATE_KEY_2 }}
          SNOWFLAKE_PRIVATE_KEY_2_PASSPHRASE: ${{ secrets.SNOWFLAKE_PRIVATE_KEY_2_PASSPHRASE }}
          SNOWFLAKE_ROLE: ${{ vars.SNOWFLAKE_ROLE }}
          SNOWFLAKE_CONNECTION_PARAMS: ${{ vars.SNOWFLAKE_CONNECTION_PARAMETERS }}
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} ${{ matrix.profile != '' && format('-P {0}', matrix.profile) || '' }} \
          -Dsnowflake.test.server.url=jdbc:snowflake://"${SNOWFLAKE_ACCOUNT_NAME}".snowflakecomputing.com/?${SNOWFLAKE_CONNECTION_PARAMS} \
          -Dsnowflake.test.server.user="${SNOWFLAKE_USER}" \
          -Dsnowflake.test.server.password="${SNOWFLAKE_PASSWORD}" \
          -Dsnowflake.test.server.private-key="${SNOWFLAKE_PRIVATE_KEY}" \
          -Dsnowflake.test.server.private-key-2="${SNOWFLAKE_PRIVATE_KEY_2}" \
          -Dsnowflake.test.server.private-key-2-passphrase="${SNOWFLAKE_PRIVATE_KEY_2_PASSPHRASE}" \
          -Dsnowflake.test.server.role="${SNOWFLAKE_ROLE}" \
          -Dsnowflake.test.warehouse=TRINO_PLUGINS_TEST_WH \
      - name: Sanitize artifact name
        if: always()
        run: |
          # Generate a valid artifact name and make it available to next steps as
          # an environment variable ARTIFACT_NAME
          # ", :, <, >, |, *, ?, \, / are not allowed in artifact names, replace it with an underscore
          name=$(echo -n "${{ matrix.modules }}" | sed -e 's/[":<>|\*\?\\\/]/_/g')
          echo "ARTIFACT_NAME=$name" >> $GITHUB_ENV
      - name: Upload test results
        uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: results ${{ env.ARTIFACT_NAME }}
          path: |
            **/target/surefire-reports
            **/target/checkstyle-*
            **/*.hprof
          retention-days: 30
      - name: Annotate with test report
        uses: trinodb/github-actions/action-surefire-report@b63800bedfbc7ab1ff2e5fe7eaecf5ab82ce6a70
        if: failure()
        with:
          # this workflow should never fail, as it is not a quality gateway
          fail_if_no_tests: false
          skip_publishing: true
      - name: Should notify on failure
        if: always() && steps.tests-nightly-snowflake-aws.outcome == 'failure'
        run: |
          # GITHUB_HEAD_REF is only populated for pull requests runs, for scheduled runs extract the name from GITHUB_REF
          branch_name="${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"
          echo "branch=$branch_name" >> "${GITHUB_OUTPUT}"
          if [[ $branch_name == "master" || $branch_name =~ ^release-.*-e$ ]]; then
            echo "result=true" >> "${GITHUB_OUTPUT}"
          else
            echo "result=false" >> "${GITHUB_OUTPUT}"
          fi
        id: should_notify
      - name: Slack notify failed tests to Connectors team
        if: always() && steps.tests-nightly-snowflake-aws.outcome && steps.should_notify.outputs.result == 'true'
        uses: archive/github-actions-slack@433fef9978d3adae73168bfc9c5a7ce722780231 # v3.1.0
        with:
          slack-bot-user-oauth-access-token: ${{ secrets.SLACK_TOKEN }}
          slack-channel: C09HT9ETS73 # galaxy-data-management-notify channel ID
          slack-text: |
            :boom: Snowflake nightly tests failed on branch: ${{ steps.should_notify.outputs.branch }} (<https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|see CI job for details>) (:thumbsup: this message if you take care of this)
      - name: Launch Flake Tracker
        if: failure() && steps.tests-nightly-snowflake-aws.outcome == 'failure' && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "::error::Cancelling merge queue workflow because hive-tests (${{ matrix.config }}) failed"
          gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  test-other-modules:
    needs: path-filters
    if: github.event_name != 'pull_request' || needs.path-filters.outputs.non_docs == 'true'
    runs-on: [self-hosted, gha-fleet-ec2-8cpu-64gb-amd64, latest]
    timeout-minutes: 60
    env:
      REPORT_TEST_SESSION_NAME: modules
      REPORT_SERVICE_NAME: other-modules
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits to be able to determine merge base for GIB
          ref: |
            ${{ github.event_name == 'repository_dispatch' &&
                github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha &&
                format('refs/pull/{0}/head', github.event.client_payload.pull_request.number) || '' }}
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
          login-docker: true
      - uses: ./.github/actions/setup
        timeout-minutes: 15
        with:
          cache: restore
          cleanup-node: true
      - name: Maven Install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} ${MAVEN_GIB} -pl '!:trino-docs,!:trino-server'
      - name: Maven Tests
        id: tests
        run: |
          $MAVEN test ${MAVEN_TEST} -pl '
            !:starburst-buffer-testing,
            !:starburst-buffer-exchange-tests,
            !:starburst-ai-client-model,
            !:starburst-base-jdbc,
            !:starburst-dynamodb,
            !:starburst-functions,
            !:starburst-functions-client,
            !:starburst-kdb,
            !:starburst-license,
            !:starburst-objectstore,
            !:starburst-oracle,
            !:starburst-salesforce,
            !:starburst-sap-hana,
            !:starburst-snowflake,
            !:starburst-sqlserver,
            !:starburst-stargate,
            !:starburst-stargate-parallel,
            !:starburst-synapse,
            !:trino-base-jdbc,
            !:trino-bigquery,
            !:trino-cassandra,
            !:trino-clickhouse,
            !:trino-delta-lake,
            !:trino-docs,
            !:trino-druid,
            !:trino-duckdb,
            !:trino-elasticsearch,
            !:trino-exasol,
            !:trino-faker,
            !:trino-faulttolerant-tests,
            !:trino-filesystem,
            !:trino-filesystem-alluxio,
            !:trino-filesystem-cache-alluxio,
            !:trino-filesystem-azure,
            !:trino-filesystem-gcs,
            !:trino-filesystem-manager,
            !:trino-filesystem-s3,
            !:trino-geospatial,
            !:trino-google-sheets,
            !:trino-hdfs,
            !:trino-hive,
            !:trino-hive-formats,
            !:trino-hudi,
            !:trino-iceberg,
            !:trino-ignite,
            !:trino-jdbc,
            !:trino-kafka,
            !:trino-kudu,
            !:trino-lakehouse,
            !:trino-main,
            !:trino-mariadb,
            !:trino-memory,
            !:trino-mongodb,
            !:trino-mysql,
            !:starburst-openapi,
            !:trino-openlineage,
            !:trino-opensearch,
            !:trino-oracle,
            !:starburst-warp-speed-core,
            !:starburst-warp-speed-extensions,
            !:starburst-warp-speed-plugin,
            !:trino-orc,
            !:trino-parquet,
            !:trino-pinot,
            !:trino-postgresql,
            !:trino-redis,
            !:trino-redshift,
            !:trino-resource-group-managers,
            !:trino-server-core,
            !:trino-server,
            !:trino-singlestore,
            !:trino-snowflake,
            !:trino-spi,
            !:trino-sqlserver,
            !:trino-test-jdbc-compatibility-old-server,
            !:trino-tests,
            !:trino-thrift,
            !:trino-vertica,
            !:trino-web-ui'

      - name: Upload test results
        uses: ./.github/actions/process-test-results
        if: always()
        with:
          has-failed-tests: ${{ steps.tests.outcome == 'failure' }}
          upload-heap-dump: ${{ env.SECRETS_PRESENT == '' && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
      - name: Upload test results to Datadog
        uses: ./.github/actions/upload-test-results-datadog
        if: always()
        with:
          datadog-api-key: ${{ secrets.DATADOG_API_KEY }}
      - name: Launch Flake Tracker
        if: failure() && steps.tests.outcome == 'failure' && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  starburst-functions-ai-tests:
    runs-on: [self-hosted, gha-fleet-ec2-4cpu-16gb-amd64, latest]
    timeout-minutes: 60
    env:
      REPORT_TEST_SESSION_NAME: modules
      REPORT_SERVICE_NAME: starburst-functions-ai
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      AZURE_OPEN_AI_LANGUAGE_ENDPOINT: ${{ vars.AZURE_OPEN_AI_LANGUAGE_ENDPOINT }}
      AZURE_OPEN_AI_EMBED_ENDPOINT: ${{ vars.AZURE_OPEN_AI_EMBED_ENDPOINT }}
      AZURE_OPEN_AI_API_KEY: ${{ secrets.AZURE_OPEN_AI_API_KEY }}
      OPEN_AI_API_KEY: ${{ secrets.OPEN_AI_API_KEY }}
      BEDROCK_ACCESS_KEY_ID: ${{ vars.BEDROCK_ACCESS_KEY_ID }}
      BEDROCK_SECRET_ACCESS_KEY: ${{ secrets.BEDROCK_SECRET_ACCESS_KEY }}
    if: |
      github.event_name == 'push' && github.ref == 'refs/heads/master' ||
      github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'ai')
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits to be able to determine merge base for GIB
          ref: |
            ${{ github.event_name == 'repository_dispatch' &&
                github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha &&
                format('refs/pull/{0}/head', github.event.client_payload.pull_request.number) || '' }}
      - name: Set up AWS code-artifacts login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
          login-docker: true
      - uses: ./.github/actions/setup
        timeout-minutes: 10
        with:
          cache: restore
          cleanup-node: true
      - name: Validate Environment Variables
        run: |
          missing_vars=()
          for var in GEMINI_API_KEY AZURE_OPEN_AI_LANGUAGE_ENDPOINT AZURE_OPEN_AI_EMBED_ENDPOINT AZURE_OPEN_AI_API_KEY OPEN_AI_API_KEY BEDROCK_ACCESS_KEY_ID BEDROCK_SECRET_ACCESS_KEY; do
            if [[ -z "${!var}" ]]; then
              missing_vars+=("$var")
            fi
          done
          if [[ ${#missing_vars[@]} -gt 0 ]]; then
            echo "Error: Required environment variables are missing: ${missing_vars[*]}"
            exit 1
          fi
      - name: Maven Install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} ${MAVEN_GIB} -pl '!:trino-docs,!:trino-server'
      - name: Maven Tests - Starburst Functions and Iceberg embedding AI Tests
        id: tests
        run: |
          $MAVEN test ${MAVEN_TEST} -pl lib/starburst-functions-client,plugin/starburst-functions -Pai-tests
          $MAVEN test ${MAVEN_TEST} -pl plugin/trino-iceberg -Pai-tests
      - name: Upload test results
        uses: ./.github/actions/process-test-results
        if: always()
        with:
          has-failed-tests: ${{ steps.tests.outcome == 'failure' }}
          upload-heap-dump: ${{ env.SECRETS_PRESENT == '' && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
      - name: Upload test results to Datadog
        uses: ./.github/actions/upload-test-results-datadog
        if: always()
        with:
          datadog-api-key: ${{ secrets.DATADOG_API_KEY }}
      - name: Launch Flake Tracker
        if: failure() && steps.tests.outcome == 'failure' && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "::error::Cancelling merge queue workflow because test-other-modules failed"
          gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  build-test-matrix:
    needs: path-filters
    if: github.event_name != 'pull_request' || needs.path-filters.outputs.non_docs == 'true'
    runs-on: [self-hosted, gha-fleet-ec2-2cpu-8gb-amd64, latest]
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits to be able to determine merge base for GIB
          ref: |
            ${{ github.event_name == 'repository_dispatch' &&
                github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha &&
                format('refs/pull/{0}/head', github.event.client_payload.pull_request.number) || '' }}
      # Only configure AWS creds here (not the full starburst-setup)
      # to avoid fetching secrets that would cause GitHub to mask "starburst" in job outputs
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@d979d5b3a71173a29b74b5b88418bfda9437d885 # v6
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-2
          role-duration-seconds: 8000
      - uses: ./.github/actions/setup
        timeout-minutes: 15
        with:
          cache: restore
      - name: Update PR check
        uses: ./.github/actions/update-check
        if: >-
          github.event_name == 'repository_dispatch' &&
          github.event.client_payload.slash_command.args.named.sha != '' &&
          github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha
        with:
          pull_request_number: ${{ github.event.client_payload.pull_request.number }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - name: Maven validate
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN validate ${MAVEN_FAST_INSTALL} ${MAVEN_GIB} -Dgib.logImpactedTo=gib-impacted.log -P disable-check-spi-dependencies
      - name: Set matrix
        id: set-matrix
        env:
          EVENT_NAME: ${{ github.event_name }}
          EVENT_SCHEDULE: ${{ github.event.schedule }}
          PR_LABELS: ${{ toJson(github.event.pull_request.labels) }}
        run: |
          # GIB doesn't run on master, so make sure the file always exist
          touch gib-impacted.log
          ./.github/bin/build-matrix-schedule-aware.py build \
            --schedule-config ./.github/schedule-config.yaml \
            --impacted gib-impacted.log \
            --event-name "$EVENT_NAME" \
            --event-schedule "$EVENT_SCHEDULE" \
            --pr-labels "$PR_LABELS" \
            --verbose > matrix.json
          echo "Matrix: $(jq '.' matrix.json)"
          echo "matrix=$(jq -c '.' matrix.json)" >> $GITHUB_OUTPUT
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "::error::Cancelling merge queue workflow because build-test-matrix failed"
          gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  test:
    runs-on: ${{ matrix.runners != null && matrix.runners || fromJSON('["self-hosted", "gha-fleet-ec2-4cpu-16gb-amd64", "latest" ]') }}
    needs: build-test-matrix
    if: needs.build-test-matrix.outputs.matrix != '{}'
    strategy:
      fail-fast: ${{ github.event_name == 'merge_group' }}
      matrix: ${{ fromJson(needs.build-test-matrix.outputs.matrix) }}
    timeout-minutes: 90
    env:
      REPORT_TEST_SESSION_NAME: test-matrix
      REPORT_SERVICE_NAME: ${{ matrix.modules }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits to be able to determine merge base for GIB
          ref: |
            ${{ github.event_name == 'repository_dispatch' &&
                github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha &&
                format('refs/pull/{0}/head', github.event.client_payload.pull_request.number) || '' }}
      - name: Set up AWS code-artifacts and ECR login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
          login-docker: true
      - uses: ./.github/actions/setup
        timeout-minutes: 15
        with:
          cache: restore
          cleanup-node: ${{ format('{0}', matrix.modules == 'plugin/trino-singlestore' || matrix.modules == 'plugin/trino-exasol' || matrix.modules == 'plugin/trino-oracle' || matrix.modules == 'plugin/trino-iceberg' || matrix.modules == 'plugin/trino-lakehouse') }}
          java-version: ${{ matrix.jdk != '' && matrix.jdk || '25.0.3' }}
      - name: Maven Install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          if [ ${{ matrix.buildAll }} == 'true' ]; then
            $MAVEN clean install ${MAVEN_FAST_INSTALL}
          else
            $MAVEN clean install ${MAVEN_FAST_INSTALL} ${MAVEN_GIB} -am -pl "${{ matrix.modules }}"
          fi
      - name: Maven Tests
        id: tests
        if: >-
          matrix.modules != 'plugin/starburst-snowflake'
          && matrix.modules != 'plugin/starburst-functions'
          && matrix.modules != 'lib/starburst-functions-client'
          && ! (contains(matrix.modules, 'starburst-objectstore') && contains(matrix.profile, 'cloud-tests'))
          && ! (contains(matrix.modules, 'trino-mongodb') && contains(matrix.profile, 'cloud-tests'))
          && ! (contains(matrix.modules, 'trino-bigquery') && contains(matrix.profile, 'cloud-tests-2'))
          && ! (contains(matrix.modules, 'trino-delta-lake') && contains(matrix.profile, 'cloud-tests'))
          && ! (contains(matrix.modules, 'trino-google-sheets') && contains(matrix.profile, 'cloud-tests'))
          && ! (contains(matrix.modules, 'trino-iceberg') && contains(matrix.profile, 'cloud-tests'))
          && ! (contains(matrix.modules, 'trino-snowflake') && contains(matrix.profile, 'cloud-tests'))
          && ! (contains(matrix.modules, 'trino-exchange-filesystem') && contains(matrix.profile, 'cloud-tests'))
          && ! (contains(matrix.modules, 'trino-filesystem-azure') && contains(matrix.profile, 'cloud-tests'))
          && ! (contains(matrix.modules, 'trino-filesystem-gcs') && contains(matrix.profile, 'cloud-tests'))
          && ! (contains(matrix.modules, 'trino-filesystem-s3') && contains(matrix.profile, 'cloud-tests'))
          && ! (contains(matrix.modules, 'starburst-buffer-data-server') && contains(matrix.profile, 'cloud-tests'))
        run: $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} ${{ matrix.profile != '' && format('-P {0}', matrix.profile) || '' }}
      # Additional tests for selected modules
      - name: Run Starburst Functions tests
        id: tests-starburst-functions
        env:
          AWS_ACCESS_KEY_ID: ${{ vars.TRINO_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TRINO_AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: us-east-1
          S3_BUCKET: ${{ vars.TRINO_S3_BUCKET }}
          GCP_STORAGE_BUCKET: ${{ vars.GCP_STORAGE_BUCKET }}
          GCP_CREDENTIALS_KEY: ${{ secrets.GCP_CREDENTIALS_KEY }}
          ABFS_CONTAINER: ${{ vars.AZURE_ABFS_HIERARCHICAL_CONTAINER }}
          ABFS_ACCOUNT: ${{ vars.AZURE_ABFS_HIERARCHICAL_ACCOUNT }}
          ABFS_ACCESS_KEY: ${{ secrets.AZURE_ABFS_HIERARCHICAL_ACCESS_KEY }}
        if: matrix.modules == 'plugin/starburst-functions' || matrix.modules == 'lib/starburst-functions-client'
        run: |
          missing_vars=()
          for var in AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY AWS_REGION S3_BUCKET GCP_STORAGE_BUCKET GCP_CREDENTIALS_KEY ABFS_CONTAINER ABFS_ACCOUNT ABFS_ACCESS_KEY; do
            if [[ -z "${!var}" ]]; then
              missing_vars+=("$var")
            fi
          done
          if [[ ${#missing_vars[@]} -gt 0 ]]; then
            echo "Error: Required environment variables are missing: ${missing_vars[*]}"
            exit 1
          fi
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} ${{ matrix.profile != '' && format('-P {0}', matrix.profile) || '' }}
      - name: HDFS file system cache isolated JVM tests
        id: tests-hdfs-isolated
        if: contains(matrix.modules, 'trino-hdfs')
        run: |
          $MAVEN test ${MAVEN_TEST} -pl :trino-hdfs -P test-isolated-jvm-suites
      - name: FileSystem Exchange Storage Cloud Tests
        id: tests-filesystem-exchange
        env:
          ABFS_FLAT_ACCOUNT: ${{ vars.AZURE_ABFS_FLAT_ACCOUNT }}
          ABFS_FLAT_ACCESS_KEY: ${{ secrets.AZURE_ABFS_FLAT_ACCESS_KEY }}
          ABFS_HIERARCHICAL_ACCOUNT: ${{ vars.AZURE_ABFS_HIERARCHICAL_ACCOUNT }}
          ABFS_HIERARCHICAL_ACCESS_KEY: ${{ secrets.AZURE_ABFS_HIERARCHICAL_ACCESS_KEY }}
        if: >-
          contains(matrix.modules, 'trino-exchange-filesystem') && contains(matrix.profile, 'cloud-tests') &&
          (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.ABFS_FLAT_ACCESS_KEY != '' || env.ABFS_HIERARCHICAL_ACCESS_KEY != '')
        run: |
          $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} ${{ format('-P {0}', matrix.profile) }}
      - name: S3 FileSystem Cloud Tests
        id: tests-s3
        env:
          AWS_ACCESS_KEY_ID: ${{ vars.TRINO_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TRINO_AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ vars.TRINO_AWS_REGION }}
        if: >-
          contains(matrix.modules, 'trino-filesystem-s3') && contains(matrix.profile, 'cloud-tests') &&
          (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.AWS_ACCESS_KEY_ID != '' || env.AWS_SECRET_ACCESS_KEY != '')
        run: |
          # Create an empty S3 bucket and multi-region access point for S3 filesystem cloud tests and add the resource names to GitHub environment variables
          .github/bin/s3/setup-empty-s3-bucket.sh
          EMPTY_S3_BUCKET=$(cat .github/bin/s3/.bucket-identifier)
          export EMPTY_S3_BUCKET
          .github/bin/s3/setup-empty-s3-mrap.sh
          AWS_MRAP_ARN=$(cat .github/bin/s3/.mrap-arn)
          export AWS_MRAP_ARN
          $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} ${{ format('-P {0}', matrix.profile) }}
      - name: Cleanup ephemeral S3 resources
        env:
          AWS_REGION: ${{ vars.TRINO_AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ vars.TRINO_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TRINO_AWS_SECRET_ACCESS_KEY }}
        # Cancelled workflows may not have cleaned up the ephemeral resources
        if: always()
        run: |
          .github/bin/s3/delete-s3-mrap.sh || true
          .github/bin/s3/delete-s3-bucket.sh || true
      - name: Cloud Object Store Tests
        id: tests-objectstore
        # Cloud tests are separate because they are time intensive, requiring cross-cloud network communication
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.TRINO_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TRINO_AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ vars.TRINO_AWS_REGION }}
          DATABRICKS_UNITY_JDBC_URL: ${{ vars.DATABRICKS_UNITY_JDBC_URL }}
          DATABRICKS_UNITY_CATALOG_NAME: ${{ vars.DATABRICKS_UNITY_CATALOG_NAME }}
          DATABRICKS_UNITY_EXTERNAL_LOCATION: ${{ vars.DATABRICKS_UNITY_EXTERNAL_LOCATION }}
          DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
          DATABRICKS_LOGIN: token
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          DATABRICKS_AWS_ACCESS_KEY_ID: ${{ vars.GALAXY_TRINO_DATABRICKS_AWS_ACCESS_KEY_ID }}
          DATABRICKS_AWS_SECRET_ACCESS_KEY: ${{ secrets.GALAXY_TRINO_DATABRICKS_AWS_SECRET_ACCESS_KEY }}
          DATABRICKS_AWS_REGION: us-east-1
        if: >-
          contains(matrix.modules, 'starburst-objectstore') && contains(matrix.profile, 'cloud-tests') &&
          (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.AWS_ACCESS_KEY_ID != '' || env.AWS_SECRET_ACCESS_KEY != '' || env.S3_BUCKET != '')
        run: |
          $MAVEN test ${MAVEN_TEST} ${{ format('-P {0}', matrix.profile) }} -pl :starburst-objectstore \
          -Ds3.bucket="galaxy-trino-ci-useast1"
      - name: Azure FileSystem Cloud Tests
        id: tests-azure
        env:
          ABFS_FLAT_ACCOUNT: ${{ vars.AZURE_ABFS_FLAT_ACCOUNT }}
          ABFS_FLAT_ACCESS_KEY: ${{ secrets.AZURE_ABFS_FLAT_ACCESS_KEY }}
          ABFS_HIERARCHICAL_ACCOUNT: ${{ vars.AZURE_ABFS_HIERARCHICAL_ACCOUNT }}
          ABFS_HIERARCHICAL_ACCESS_KEY: ${{ secrets.AZURE_ABFS_HIERARCHICAL_ACCESS_KEY }}
          ABFS_OAUTH_TENANT_ID: ${{ vars.AZURE_ABFS_OAUTH_TENANT_ID }}
          ABFS_OAUTH_CLIENT_ID: ${{ vars.AZURE_ABFS_OAUTH_CLIENT_ID }}
          ABFS_OAUTH_CLIENT_SECRET: ${{ secrets.AZURE_ABFS_OAUTH_CLIENT_SECRET }}
        # Run tests if any of the secrets are present
        if: >-
          contains(matrix.modules, 'trino-filesystem-azure') && contains(matrix.profile, 'cloud-tests') &&
          (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.ABFS_FLAT_ACCESS_KEY != '' || env.ABFS_HIERARCHICAL_ACCESS_KEY != '' || env.ABFS_OAUTH_CLIENT_SECRET != '')
        run: |
          $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} ${{ format('-P {0}', matrix.profile) }}
      - name: GCS FileSystem Cloud Tests
        id: tests-gcs
        env:
          GCP_CREDENTIALS_KEY: ${{ secrets.GCP_CREDENTIALS_KEY }}
        if: >-
          contains(matrix.modules, 'trino-filesystem-gcs') && contains(matrix.profile, 'cloud-tests') &&
          (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.GCP_CREDENTIALS_KEY != '')
        run: |
          $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} ${{ format('-P {0}', matrix.profile) }}
      - name: Buffer Service TrinoFs Spooling Cloud Tests
        id: tests-buffer-trino-fs-cloud
        env:
          AWS_ACCESS_KEY_ID: ${{ vars.TRINO_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TRINO_AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ vars.TRINO_AWS_REGION }}
          S3_BUCKET: ${{ vars.TRINO_S3_BUCKET }}
          ABFS_FLAT_ACCOUNT: ${{ vars.AZURE_ABFS_FLAT_ACCOUNT }}
          ABFS_FLAT_ACCESS_KEY: ${{ secrets.AZURE_ABFS_FLAT_ACCESS_KEY }}
          ABFS_HIERARCHICAL_ACCOUNT: ${{ vars.AZURE_ABFS_HIERARCHICAL_ACCOUNT }}
          ABFS_HIERARCHICAL_ACCESS_KEY: ${{ secrets.AZURE_ABFS_HIERARCHICAL_ACCESS_KEY }}
          ABFS_OAUTH_TENANT_ID: ${{ vars.AZURE_ABFS_OAUTH_TENANT_ID }}
          ABFS_OAUTH_CLIENT_ID: ${{ vars.AZURE_ABFS_OAUTH_CLIENT_ID }}
          ABFS_OAUTH_CLIENT_SECRET: ${{ secrets.AZURE_ABFS_OAUTH_CLIENT_SECRET }}
          GCP_CREDENTIALS_KEY: ${{ secrets.GCP_CREDENTIALS_KEY }}
          GCP_STORAGE_BUCKET: ${{ vars.GCP_STORAGE_BUCKET }}
        if: >-
          contains(matrix.modules, 'starburst-buffer-data-server') && contains(matrix.profile, 'cloud-tests') &&
          (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.AWS_ACCESS_KEY_ID != '' || env.ABFS_FLAT_ACCESS_KEY != '' || env.ABFS_HIERARCHICAL_ACCESS_KEY != '' || env.ABFS_OAUTH_CLIENT_SECRET != '' || env.GCP_CREDENTIALS_KEY != '')
        run: |
          $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} ${{ format('-P {0}', matrix.profile) }}
      - name: Cloud Delta Lake Tests
        id: tests-delta
      # Cloud tests are separate because they are time intensive, requiring cross-cloud network communication
        env:
          ABFS_CONTAINER: ${{ vars.AZURE_ABFS_HIERARCHICAL_CONTAINER }}
          ABFS_ACCOUNT: ${{ vars.AZURE_ABFS_HIERARCHICAL_ACCOUNT }}
          ABFS_ACCESSKEY: ${{ secrets.AZURE_ABFS_HIERARCHICAL_ACCESS_KEY }}
          AWS_ACCESS_KEY_ID: ${{ vars.TRINO_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TRINO_AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ vars.TRINO_AWS_REGION }}
          S3_BUCKET: ${{ vars.TRINO_S3_BUCKET }}
          GCP_CREDENTIALS_KEY: ${{ secrets.GCP_CREDENTIALS_KEY }}
          GCP_STORAGE_BUCKET: ${{ vars.GCP_STORAGE_BUCKET }}
          GCS_DATABRICKS_UNITY_JDBC_URL: ${{ vars.GCS_DATABRICKS_UNITY_JDBC_URL }}
          GCS_DATABRICKS_UNITY_CATALOG_NAME: ${{ vars.GCS_DATABRICKS_UNITY_CATALOG_NAME }}
          GCS_DATABRICKS_UNITY_EXTERNAL_LOCATION: ${{ vars.GCS_DATABRICKS_UNITY_EXTERNAL_LOCATION }}
          GCS_DATABRICKS_HOST: ${{ vars.GCS_DATABRICKS_HOST }}
          GCS_DATABRICKS_LOGIN: token
          GCS_DATABRICKS_TOKEN: ${{ secrets.GCS_DATABRICKS_TOKEN }}
          DATABRICKS_UNITY_GCP_GCS_JSON_KEY: ${{ secrets.DATABRICKS_UNITY_GCP_GCS_JSON_KEY }}
          DATABRICKS_UNITY_JDBC_URL: ${{ vars.DATABRICKS_UNITY_JDBC_URL }}
          DATABRICKS_UNITY_CATALOG_NAME: ${{ vars.DATABRICKS_UNITY_CATALOG_NAME }}
          DATABRICKS_UNITY_EXTERNAL_LOCATION: ${{ vars.DATABRICKS_UNITY_EXTERNAL_LOCATION }}
          AZURE_DATABRICKS_UNITY_JDBC_URL: ${{ vars.AZURE_DATABRICKS_UNITY_JDBC_URL }}
          AZURE_DATABRICKS_UNITY_CATALOG_NAME: ${{ vars.AZURE_DATABRICKS_UNITY_CATALOG_NAME }}
          AZURE_DATABRICKS_UNITY_EXTERNAL_LOCATION: ${{ vars.AZURE_DATABRICKS_UNITY_EXTERNAL_LOCATION }}
          AZURE_DATABRICKS_HOST: ${{ vars.AZURE_DATABRICKS_HOST }}
          AZURE_DATABRICKS_LOGIN: token
          AZURE_DATABRICKS_TOKEN: ${{ secrets.AZURE_DATABRICKS_TOKEN }}
          DATABRICKS_AZURE_STORAGE_ACCESS_KEY: ${{ secrets.DATABRICKS_AZURE_STORAGE_ACCESS_KEY }}
          DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
          DATABRICKS_LOGIN: token
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          DATABRICKS_AWS_ACCESS_KEY_ID: ${{ vars.GALAXY_TRINO_DATABRICKS_AWS_ACCESS_KEY_ID }}
          DATABRICKS_AWS_SECRET_ACCESS_KEY: ${{ secrets.GALAXY_TRINO_DATABRICKS_AWS_SECRET_ACCESS_KEY }}
          DATABRICKS_AWS_REGION: us-east-1
        # Run tests if any of the secrets is present. Do not skip tests when one secret renamed, or secret name has a typo.
        if: >-
          contains(matrix.modules, 'trino-delta-lake') && contains(matrix.profile, 'cloud-tests') &&
          (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.ABFS_ACCOUNT != '' || env.ABFS_CONTAINER != '' || env.ABFS_ACCESSKEY != '' || env.AWS_ACCESS_KEY_ID != '' || env.AWS_SECRET_ACCESS_KEY != '' || env.GCP_CREDENTIALS_KEY != '')
        run: |
          $MAVEN test ${MAVEN_TEST} ${{ format('-P {0}', matrix.profile) }} -pl :trino-delta-lake \
            -Dtesting.azure-abfs-container="${ABFS_CONTAINER}" \
            -Dtesting.azure-abfs-account="${ABFS_ACCOUNT}" \
            -Dtesting.azure-abfs-access-key="${ABFS_ACCESSKEY}" \
            -Dtesting.gcp-storage-bucket="${GCP_STORAGE_BUCKET}" \
            -Dtesting.gcp-credentials-key="${GCP_CREDENTIALS_KEY}"
      - name: Cloud MongoDB Tests
        id: tests-mongodb
        if: matrix.modules == 'plugin/trino-mongodb' && contains(matrix.profile, 'cloud-tests') && (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.MONGODB_ATLAS_CONNECTION_URL != '')
        env:
          MONGODB_CONFIG: >-
            -Dtest.mongodb.atlas.connection-url=${{ secrets.MONGODB_ATLAS_CONNECTION_URL }}
            -Dtest.mongodb.federated-database.connection-url=${{ secrets.MONGODB_FEDERATED_DATABASE_CONNECTION_URL }}
            -Dtest.mongodb.federated-datasource.connection-url=${{ secrets.MONGODB_FEDERATED_DATASOURCE_CONNECTION_URL }}
            -Dtest.mongodb.federated-datasource.cluster-name=${{ vars.MONGODB_FEDERATED_DATASOURCE_CLUSTER_NAME }}
            -Dtest.mongodb.federated-datasource.project-id=${{ vars.MONGODB_FEDERATED_DATASOURCE_PROJECT_ID }}
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN test ${MAVEN_TEST} -pl :trino-mongodb -Pcloud-tests ${MONGODB_CONFIG}
      - name: Cloud BigQuery Tests
        id: tests-bq
        env:
          BIGQUERY_CREDENTIALS_KEY: ${{ secrets.BIGQUERY_CREDENTIALS_KEY }}
          BIGQUERY_CASE_INSENSITIVE_CREDENTIALS_KEY: ${{ secrets.BIGQUERY_CASE_INSENSITIVE_CREDENTIALS_KEY }}
          GCP_STORAGE_BUCKET: ${{ vars.GCP_STORAGE_BUCKET }}
          BIGQUERY_TESTING_BIGLAKE_CONNECTION_ID: ${{ vars.BIGQUERY_TESTING_BIGLAKE_CONNECTION_ID }}
          BIGQUERY_TESTING_PROJECT_ID: ${{ vars.BIGQUERY_TESTING_PROJECT_ID }}
          BIGQUERY_TESTING_PARENT_PROJECT_ID: ${{ vars.BIGQUERY_TESTING_PARENT_PROJECT_ID }}
        if: matrix.modules == 'plugin/trino-bigquery' && !contains(matrix.profile, 'cloud-tests-2') && (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.BIGQUERY_CREDENTIALS_KEY != '')
        run: |
          $MAVEN test ${MAVEN_TEST} -pl :trino-bigquery -Pcloud-tests-1 \
            -Dtesting.bigquery.credentials-key="${BIGQUERY_CREDENTIALS_KEY}" \
            -Dtesting.bigquery-case-insensitive.credentials-key="${BIGQUERY_CASE_INSENSITIVE_CREDENTIALS_KEY}" \
            -Dtesting.gcp-storage-bucket="${GCP_STORAGE_BUCKET}" \
            -Dtesting.bigquery-connection-id="${BIGQUERY_TESTING_BIGLAKE_CONNECTION_ID}" \
            -Dtesting.bigquery-project-id="${BIGQUERY_TESTING_PROJECT_ID}" \
            -Dtesting.bigquery-parent-project-id="${BIGQUERY_TESTING_PARENT_PROJECT_ID}"
      - name: Cloud BigQuery Smoke Tests
        id: tests-bq-smoke
        env:
          BIGQUERY_CREDENTIALS_KEY: ${{ secrets.BIGQUERY_CREDENTIALS_KEY }}
          GCP_STORAGE_BUCKET: ${{ vars.GCP_STORAGE_BUCKET }}
        if: matrix.modules == 'plugin/trino-bigquery' && contains(matrix.profile, 'cloud-tests-2') && (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.BIGQUERY_CREDENTIALS_KEY != '')
        run: |
          $MAVEN test ${MAVEN_TEST} -pl :trino-bigquery -Pcloud-tests-2 \
            -Dtesting.bigquery.credentials-key="${BIGQUERY_CREDENTIALS_KEY}" \
            -Dtesting.gcp-storage-bucket="${GCP_STORAGE_BUCKET}" \
            -Dtesting.alternate-bq-project-id=bigquery-cicd-alternate
      - name: Cloud Snowflake Tests
        id: tests-snowflake
        env:
          SNOWFLAKE_URL: ${{ vars.SNOWFLAKE_URL }}
          SNOWFLAKE_USER: ${{ vars.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_PRIVATE_KEY: ${{ secrets.SNOWFLAKE_PRIVATE_KEY }}
          SNOWFLAKE_PRIVATE_KEY_2: ${{ secrets.SNOWFLAKE_PRIVATE_KEY_2 }}
          SNOWFLAKE_PRIVATE_KEY_2_PASSPHRASE: ${{ secrets.SNOWFLAKE_PRIVATE_KEY_2_PASSPHRASE }}
          SNOWFLAKE_CONNECTION_PARAMS: ${{ vars.SNOWFLAKE_CONNECTION_PARAMETERS }}
          SNOWFLAKE_DATABASE: ${{ vars.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_ROLE: ${{ vars.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ vars.SNOWFLAKE_WAREHOUSE }}
        if: matrix.modules == 'plugin/trino-snowflake' && contains(matrix.profile, 'cloud-tests') && (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.SNOWFLAKE_URL != '')
        run: |
          $MAVEN test ${MAVEN_TEST} -pl :trino-snowflake -Pcloud-tests \
            -Dsnowflake.test.server.url="${SNOWFLAKE_URL}?${SNOWFLAKE_CONNECTION_PARAMS}" \
            -Dsnowflake.test.server.user="${SNOWFLAKE_USER}" \
            -Dsnowflake.test.server.password="${SNOWFLAKE_PASSWORD}" \
            -Dsnowflake.test.server.private-key="${SNOWFLAKE_PRIVATE_KEY}" \
            -Dsnowflake.test.server.private-key-2="${SNOWFLAKE_PRIVATE_KEY_2}" \
            -Dsnowflake.test.server.private-key-2-passphrase="${SNOWFLAKE_PRIVATE_KEY_2_PASSPHRASE}" \
            -Dsnowflake.test.server.database="${SNOWFLAKE_DATABASE}" \
            -Dsnowflake.test.server.role="${SNOWFLAKE_ROLE}" \
            -Dsnowflake.test.server.warehouse="${SNOWFLAKE_WAREHOUSE}"
      - name: Iceberg Cloud Tests
        id: tests-iceberg
        env:
          AWS_ACCESS_KEY_ID: ${{ vars.TRINO_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TRINO_AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ vars.TRINO_AWS_REGION }}
          S3_BUCKET: ${{ vars.TRINO_S3_BUCKET }}
          S3_TABLES_BUCKET: ${{ vars.S3_TABLES_BUCKET }}
          S3_TABLES_AWS_ACCESS_KEY_ID: ${{ vars.S3_TABLES_AWS_ACCESS_KEY_ID }}
          S3_TABLES_AWS_SECRET_ACCESS_KEY: ${{ secrets.S3_TABLES_AWS_SECRET_ACCESS_KEY }}
          S3_TABLES_AWS_REGION: ${{ vars.S3_TABLES_AWS_REGION }}
          GCP_CREDENTIALS_KEY: ${{ secrets.GCP_CREDENTIALS_KEY }}
          GCP_STORAGE_BUCKET: ${{ vars.GCP_STORAGE_BUCKET }}
          ABFS_CONTAINER: ${{ vars.AZURE_ABFS_HIERARCHICAL_CONTAINER }}
          ABFS_ACCOUNT: ${{ vars.AZURE_ABFS_HIERARCHICAL_ACCOUNT }}
          ABFS_ACCESS_KEY: ${{ secrets.AZURE_ABFS_HIERARCHICAL_ACCESS_KEY }}
          SNOWFLAKE_USER: ${{ vars.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_URL: ${{ vars.SNOWFLAKE_URL }}
          SNOWFLAKE_DATABASE: ${{ vars.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_CATALOG_SCHEMA: ${{ vars.SNOWFLAKE_CATALOG_SCHEMA }}
          SNOWFLAKE_ROLE: ${{ vars.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ vars.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_CATALOG_S3_ACCESS_KEY_ID: ${{ vars.SNOWFLAKE_CATALOG_S3_ACCESS_KEY_ID }}
          SNOWFLAKE_CATALOG_S3_SECRET_ACCESS_KEY: ${{ secrets.SNOWFLAKE_CATALOG_S3_SECRET_ACCESS_KEY }}
          SNOWFLAKE_EXTERNAL_VOLUME: ${{ vars.SNOWFLAKE_EXTERNAL_VOLUME }}
          SNOWFLAKE_CATALOG_S3_REGION: ${{ vars.SNOWFLAKE_CATALOG_S3_REGION }}
          DATABRICKS_UNITY_JDBC_URL: ${{ vars.DATABRICKS_UNITY_JDBC_URL }}
          DATABRICKS_UNITY_CATALOG_NAME: ${{ vars.DATABRICKS_UNITY_CATALOG_NAME }}
          DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
          DATABRICKS_LOGIN: token
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          DATABRICKS_AWS_ACCESS_KEY_ID: ${{ vars.GALAXY_TRINO_DATABRICKS_AWS_ACCESS_KEY_ID }}
          DATABRICKS_AWS_SECRET_ACCESS_KEY: ${{ secrets.GALAXY_TRINO_DATABRICKS_AWS_SECRET_ACCESS_KEY }}
          DATABRICKS_AWS_REGION: us-east-1
        if: >-
          contains(matrix.modules, 'trino-iceberg') && contains(matrix.profile, 'cloud-tests') &&
          (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.AWS_ACCESS_KEY_ID != '' || env.AWS_SECRET_ACCESS_KEY != '' || env.GCP_CREDENTIALS_KEY != '')
        run: |
          $MAVEN test ${MAVEN_TEST} -pl :trino-iceberg ${{ format('-P {0}', matrix.profile) }} \
            -Dtesting.gcp-storage-bucket="${GCP_STORAGE_BUCKET}" \
            -Dtesting.gcp-credentials-key="${GCP_CREDENTIALS_KEY}" \
            -Dtesting.azure-abfs-container="${ABFS_CONTAINER}" \
            -Dtesting.azure-abfs-account="${ABFS_ACCOUNT}" \
            -Dtesting.azure-abfs-access-key="${ABFS_ACCESS_KEY}" \
            -Dtesting.snowflake.catalog.user="${SNOWFLAKE_USER}" \
            -Dtesting.snowflake.catalog.password="${SNOWFLAKE_PASSWORD}" \
            -Dtesting.snowflake.catalog.account-url="${SNOWFLAKE_URL}" \
            -Dtesting.snowflake.catalog.database="${SNOWFLAKE_DATABASE}" \
            -Dtesting.snowflake.catalog.schema="${SNOWFLAKE_CATALOG_SCHEMA}" \
            -Dtesting.snowflake.catalog.role="${SNOWFLAKE_ROLE}" \
            -Dtesting.snowflake.catalog.warehouse="${SNOWFLAKE_WAREHOUSE}" \
            -Dtesting.snowflake.catalog.s3.access-key="${SNOWFLAKE_CATALOG_S3_ACCESS_KEY_ID}" \
            -Dtesting.snowflake.catalog.s3.secret-key="${SNOWFLAKE_CATALOG_S3_SECRET_ACCESS_KEY}" \
            -Dtesting.snowflake.catalog.s3.external.volume="${SNOWFLAKE_EXTERNAL_VOLUME}" \
            -Dtesting.snowflake.catalog.s3.region="${SNOWFLAKE_CATALOG_S3_REGION}"
      - name: Cloud Google Sheets Tests
        id: tests-google-sheets
        env:
          SHEETS_CREDENTIALS_KEY: ${{ secrets.SHEETS_CREDENTIALS_KEY }}
        if: contains(matrix.modules, 'trino-google-sheets') && contains(matrix.profile, 'cloud-tests') && (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.SHEETS_CREDENTIALS_KEY != '')
        run: |
          $MAVEN test ${MAVEN_TEST} -pl :trino-google-sheets -Pcloud-tests \
            -Dtesting.gsheets.credentials-key="${SHEETS_CREDENTIALS_KEY}"
      - name: Cloud Redshift Tests ${{ matrix.profile }}
        id: tests-redshift
        env:
          AWS_REGION: ${{ vars.REDSHIFT_AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ vars.REDSHIFT_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.REDSHIFT_AWS_SECRET_ACCESS_KEY }}
          REDSHIFT_SUBNET_GROUP_NAME: ${{ vars.REDSHIFT_SUBNET_GROUP_NAME }}
          REDSHIFT_IAM_ROLES: ${{ vars.REDSHIFT_IAM_ROLES }}
          REDSHIFT_VPC_SECURITY_GROUP_IDS: ${{ vars.REDSHIFT_VPC_SECURITY_GROUP_IDS }}
          REDSHIFT_S3_TPCH_TABLES_ROOT: ${{ vars.REDSHIFT_S3_TPCH_TABLES_ROOT }}
          REDSHIFT_S3_UNLOAD_ROOT: ${{ vars.REDSHIFT_S3_UNLOAD_ROOT }}
        if: >-
          contains(matrix.modules, 'trino-redshift') &&
          (contains(matrix.profile, 'cloud-tests') || contains(matrix.profile, 'fte-tests')) &&
          (env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || env.AWS_ACCESS_KEY_ID != '' || env.REDSHIFT_SUBNET_GROUP_NAME != '')
        run: |
          source .github/bin/redshift/setup-aws-redshift.sh

          $MAVEN test ${MAVEN_TEST} -pl ${{ matrix.modules }} ${{ format('-P {0}', matrix.profile) }} \
            -Dtest.redshift.jdbc.user="${REDSHIFT_USER}" \
            -Dtest.redshift.jdbc.password="${REDSHIFT_PASSWORD}" \
            -Dtest.redshift.jdbc.endpoint="${REDSHIFT_ENDPOINT}:${REDSHIFT_PORT}/" \
            -Dtest.redshift.s3.tpch.tables.root="${REDSHIFT_S3_TPCH_TABLES_ROOT}" \
            -Dtest.redshift.s3.unload.root="${REDSHIFT_S3_UNLOAD_ROOT}" \
            -Dtest.redshift.iam.role="${REDSHIFT_IAM_ROLES}" \
            -Dtest.redshift.aws.region="${AWS_REGION}" \
            -Dtest.redshift.aws.access-key="${AWS_ACCESS_KEY_ID}" \
            -Dtest.redshift.aws.secret-key="${AWS_SECRET_ACCESS_KEY}"
      - name: Cleanup ephemeral Redshift Cluster
        env:
          AWS_REGION: ${{ vars.REDSHIFT_AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ vars.REDSHIFT_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.REDSHIFT_AWS_SECRET_ACCESS_KEY }}
        # Cancelled workflows may have left the ephemeral cluster running
        if: always()
        run: .github/bin/redshift/delete-aws-redshift.sh
      - name: Sanitize artifact name
        if: always()
        run: |
          # Generate a valid artifact name and make it available to next steps as
          # an environment variable ARTIFACT_NAME
          # ", :, <, >, |, *, ?, \, / are not allowed in artifact names, replace it with an underscore
          name=$(echo -n "${{ matrix.modules }}, ${{ matrix.profile }}, ${{ matrix.jdk }}, ${{ matrix.cloud }}" | sed -e 's/[":<>|\*\?\\\/]/_/g')
          # final artifact name can't be longer than 128 characters
          echo "ARTIFACT_NAME=${name:0:100}" >> $GITHUB_ENV
      - name: Upload test results
        uses: ./.github/actions/process-test-results
        if: always()
        with:
          artifact-name: ${{ env.ARTIFACT_NAME }}
          has-failed-tests: >-
            ${{ steps.tests.outcome == 'failure'
              || steps.tests-starburst-functions.outcome == 'failure'
              || steps.tests-hdfs-isolated.outcome == 'failure'
              || steps.tests-s3.outcome == 'failure'
              || steps.tests-azure.outcome == 'failure'
              || steps.tests-buffer-trino-fs-cloud.outcome == 'failure'
              || steps.tests-filesystem-exchange.outcome == 'failure'
              || steps.tests-gcs.outcome == 'failure'
              || steps.tests-delta.outcome == 'failure'
              || steps.tests-objectstore.outcome == 'failure'
              || steps.tests-mongodb.outcome == 'failure'
              || steps.tests-bq.outcome == 'failure'
              || steps.tests-bq-smoke.outcome == 'failure'
              || steps.tests-google-sheets.outcome == 'failure'
              || steps.tests-iceberg.outcome == 'failure'
              || steps.tests-redshift.outcome == 'failure'
              || steps.tests-snowflake.outcome == 'failure'
            }}
          upload-heap-dump: ${{ env.SECRETS_PRESENT == '' && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
      - id: upload-test-results-datadog
        name: Upload test results to Datadog
        uses: ./.github/actions/upload-test-results-datadog
        if: always()
        with:
          datadog-api-key: ${{ secrets.DATADOG_API_KEY }}
      - name: Launch Flake Tracker
        if: failure() && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Update PR check
        uses: ./.github/actions/update-check
        if: >-
          failure() &&
          github.event_name == 'repository_dispatch' &&
          github.event.client_payload.slash_command.args.named.sha != '' &&
          github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha
        with:
          pull_request_number: ${{ github.event.client_payload.pull_request.number }}
          check_name: ${{ github.job }} with secrets
          conclusion: ${{ job.status }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "::error::Cancelling merge queue workflow because test (${{ matrix.modules }}, ${{ matrix.profile }}, ${{ matrix.jdk }}) failed"
          gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  gpu-tests:
    runs-on: [self-hosted, gha-fleet-ec2-1GPU-16CPU-64GB-amd64, latest]
    if: |
      github.event_name != 'pull_request' ||
      contains(github.event.pull_request.labels.*.name, 'gpu')
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: |
            ${{ github.event_name == 'repository_dispatch' &&
                github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha &&
                format('refs/pull/{0}/head', github.event.client_payload.pull_request.number) || '' }}
      - name: Set up AWS code-artifacts and ECR login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
          login-docker: true
      - uses: ./.github/actions/setup
      - name: Maven Install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install -DskipTests -Dmaven.javadoc.skip=true -Dmaven.source.skip=true -Dair.check.skip-all=true -Dskip.npm -Dskip.yarn -pl :trino-main,:trino-hive,:trino-tests -am
      - name: Maven Tests - core/trino-main
        run: $MAVEN test ${MAVEN_TEST} -pl core/trino-main -P gpu
      - name: Maven Tests - plugin/trino-hive
        run: $MAVEN test ${MAVEN_TEST} -pl plugin/trino-hive -P gpu
      - name: Maven Tests - testing/trino-tests
        run: $MAVEN test ${MAVEN_TEST} -pl testing/trino-tests -P gpu
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  build-pt:
    needs: path-filters
    if: github.event_name != 'pull_request' || needs.path-filters.outputs.non_docs == 'true'
    runs-on: [self-hosted, gha-fleet-ec2-8cpu-64gb-amd64, latest]
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
      product-tests-changed: ${{ steps.filter.outputs.product-tests }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits to be able to determine merge base for GIB
          ref: |
            ${{ github.event_name == 'repository_dispatch' &&
                github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha &&
                format('refs/pull/{0}/head', github.event.client_payload.pull_request.number) || '' }}
      # Only configure AWS creds here (not the full starburst-setup)
      # to avoid fetching secrets that would cause GitHub to mask "starburst" in job outputs
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@d979d5b3a71173a29b74b5b88418bfda9437d885 # v6
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-2
          role-duration-seconds: 8000
      - uses: ./.github/actions/setup
        timeout-minutes: 15
        with:
          cache: restore
          cleanup-node: true
      - uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d # v4
        id: filter
        with:
          filters: |
            product-tests:
              - 'testing/trino-product-tests*/**'
              - 'testing/trino-testing-services/**'
              # run all tests when there are any changes in the trino-server Maven module
              # because it doesn't define it's Trino dependencies and
              # it relies on the Provisio plugin to find the right artifacts
              - 'core/trino-server/**'
              - '.github/**'
      - name: Maven Install
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          $MAVEN clean install ${MAVEN_FAST_INSTALL} -pl '!:trino-docs'
      - name: Map impacted plugins to features
        run: |
          export MAVEN_OPTS="${MAVEN_INSTALL_OPTS}"
          # build a list of impacted modules, ignoring modules that cannot affect either product tests or Trino
          $MAVEN validate ${MAVEN_FAST_INSTALL} ${MAVEN_GIB} -Dgib.logImpactedTo=gib-impacted.log -pl '!:trino-docs,!:trino-tests,!:trino-faulttolerant-tests'
          # GIB doesn't run on master, so make sure the file always exist
          touch gib-impacted.log
          testing/trino-plugin-reader/target/trino-plugin-reader-*-executable.jar -i gib-impacted.log -p core/trino-server/target/trino-server-*-hardlinks/plugin > impacted-features.log
          echo "Impacted plugin features:"
          cat impacted-features.log
      - name: Product tests artifact
        uses: actions/upload-artifact@v7
        with:
          name: product tests and server tarball
          path: |
            core/trino-server/target/*.tar.gz
            impacted-features.log
            testing/trino-product-tests-launcher/target/*.jar
            testing/trino-product-tests/target/*-executable.jar
            client/trino-cli/target/*-executable.jar
          retention-days: 1
      - id: prepare-matrix-template
        run: |
          cat <<EOF > .github/test-pt-matrix.yaml
          config:
            - default
          suite:
            # Starburst suites
            - suite-objectstore
            - suite-sap-hana
            - suite-stargate
            - suite-starburst-snowflake
            #- suite-synapse # Synapse suite is run separately in synapse-tests job
            - suite-open-api
            # End of Starburst suites
            - suite-1
            - suite-2
            - suite-3
            # suite-4 does not exist
            - suite-5
            - suite-6-non-generic
            - suite-7-non-generic
            - suite-hive-transactional
            - suite-azure
            - suite-delta-lake-databricks133
            - suite-delta-lake-databricks143
            - suite-delta-lake-databricks154
            - suite-delta-lake-databricks164
            - suite-delta-lake-databricks173
            - suite-delta-lake-databricks-unity
            - suite-delta-lake-databricks-unity-s3-credentials-vending
            - suite-delta-lake-databricks-unity-gcs-credentials-vending
            - suite-hive-databricks-unity
            #- suite-exasol
            - suite-ranger
            - suite-gcs
            - suite-hive4
            - suite-clients
            - suite-functions
            - suite-kdb
            - suite-tpch
            - suite-tpcds
            - suite-storage-formats-detailed
            - suite-parquet
            - suite-oauth2
            - suite-ldap
            - suite-loki
            - suite-compatibility
            - suite-all-connectors-smoke
            - suite-delta-lake-oss
            - suite-kafka
            - suite-cassandra
            - suite-clickhouse
            - suite-mysql
            - suite-iceberg
            #- suite-snowflake # This is OSS Snowflake connector suite. We run Starburst Snowflake suite instead.
            - suite-hudi
            - suite-ignite
          exclude:
            - suite: suite-azure
              ignore exclusion if: >-
                ${{ env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' ||
                    vars.AZURE_ABFS_HIERARCHICAL_CONTAINER != '' ||
                    vars.AZURE_ABFS_HIERARCHICAL_ACCOUNT != '' ||
                    secrets.AZURE_ABFS_HIERARCHICAL_ACCESS_KEY != '' }}

            - suite: suite-gcs
              ignore exclusion if: >-
                ${{ env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || secrets.GCP_CREDENTIALS_KEY != '' }}

            - suite: suite-delta-lake-databricks133
              ignore exclusion if: >-
                ${{ (github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') ||
                    (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'databricks')) ||
                    (github.event_name == 'workflow_dispatch') ||
                    (env.CI_SKIP_SECRETS_PRESENCE_CHECKS == '' && secrets.DATABRICKS_TOKEN == '') }}
            - suite: suite-delta-lake-databricks143
              ignore exclusion if: >-
                ${{ (github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') ||
                    (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'databricks')) ||
                    (github.event_name == 'workflow_dispatch') ||
                    (env.CI_SKIP_SECRETS_PRESENCE_CHECKS == '' && secrets.DATABRICKS_TOKEN == '') }}
            - suite: suite-delta-lake-databricks154
              ignore exclusion if: >-
                ${{ (github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') ||
                    (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'databricks')) ||
                    (github.event_name == 'workflow_dispatch') ||
                    (env.CI_SKIP_SECRETS_PRESENCE_CHECKS == '' && secrets.DATABRICKS_TOKEN == '') }}
            - suite: suite-delta-lake-databricks164
              ignore exclusion if: >-
                ${{ (github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') ||
                    (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'databricks')) ||
                    (github.event_name == 'workflow_dispatch') ||
                    (env.CI_SKIP_SECRETS_PRESENCE_CHECKS == '' && secrets.DATABRICKS_TOKEN == '') }}
            - suite: suite-delta-lake-databricks173
              ignore exclusion if: >-
                ${{ (github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') ||
                    (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'databricks')) ||
                    (github.event_name == 'workflow_dispatch') ||
                    (env.CI_SKIP_SECRETS_PRESENCE_CHECKS == '' && secrets.DATABRICKS_TOKEN == '') }}
            - suite: suite-delta-lake-databricks-unity
              config: hdp3
            - suite: suite-delta-lake-databricks-unity
              ignore exclusion if: >-
                ${{ (github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') ||
                    (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'databricks')) ||
                    (github.event_name == 'workflow_dispatch') ||
                    (env.CI_SKIP_SECRETS_PRESENCE_CHECKS == '' && secrets.DATABRICKS_TOKEN == '') }}
            - suite: suite-delta-lake-databricks-unity-s3-credentials-vending
              config: hdp3
            - suite: suite-delta-lake-databricks-unity-s3-credentials-vending
              ignore exclusion if: >-
                ${{ (github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') ||
                    (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'databricks')) ||
                    (github.event_name == 'workflow_dispatch') ||
                    (env.CI_SKIP_SECRETS_PRESENCE_CHECKS == '' && secrets.DATABRICKS_TOKEN == '') }}
            - suite: suite-delta-lake-databricks-unity-gcs-credentials-vending
              config: hdp3
            - suite: suite-delta-lake-databricks-unity-gcs-credentials-vending
              ignore exclusion if: >-
                ${{ (github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') ||
                    (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'databricks')) ||
                    (github.event_name == 'workflow_dispatch') ||
                    (env.CI_SKIP_SECRETS_PRESENCE_CHECKS == '' && secrets.DATABRICKS_TOKEN == '') }}
            - suite: suite-hive-databricks-unity
              config: hdp3
            - suite: suite-hive-databricks-unity
              ignore exclusion if: >-
                ${{ (github.event_name == 'schedule' && github.event.schedule == '0 0 * * *') ||
                    (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'databricks')) ||
                    (github.event_name == 'workflow_dispatch') ||
                    (env.CI_SKIP_SECRETS_PRESENCE_CHECKS == '' && secrets.DATABRICKS_TOKEN == '') }}
            - suite: suite-snowflake
              ignore exclusion if: >-
                ${{ env.CI_SKIP_SECRETS_PRESENCE_CHECKS != '' || secrets.SNOWFLAKE_PASSWORD != '' || env.SNOWFLAKE_PRIVATE_KEY != '' }}

          ignore exclusion if:
            # Do not use this property outside of the matrix configuration.
            #
            # This is added to all matrix entries so they may be conditionally
            # excluded by adding them to the excludes list with a GHA expression
            # for this property.
            # - If the expression evaluates to true, it will never match the a
            #   actual value of the property, and will therefore not be excluded.
            # - If the expression evaluates to false, it will match the actual
            #   value of the property, and the exclusion will apply normally.
            - "false"
          include:
            # this suite is designed specifically for apache-hive3. TODO remove the suite once we can run all regular tests on apache-hive3.
            - config: apache-hive3
              suite: suite-hms-only
            # Workaround for https://github.com/microsoft/mssql-docker/issues/899
            - config: default
              suite: suite-7-non-generic
              runson: ubuntu-20.04
            - config: default
              suite: suite-warp-speed
              runners: [ self-hosted, gha-fleet-ec2-8cpu-64gb-amd64, latest ]
            - config: default
              suite: suite-warp-speed-iceberg
              runners: [ self-hosted, gha-fleet-ec2-8cpu-64gb-amd64, latest ]
            - config: default
              suite: suite-warp-speed-delta-lake
              runners: [ self-hosted, gha-fleet-ec2-8cpu-64gb-amd64, latest ]

          EOF
      - name: Build PT matrix (all)
        if: |
          github.event_name != 'pull_request' ||
          steps.filter.outputs.product-tests == 'true' ||
          contains(github.event.pull_request.labels.*.name, 'tests:all') ||
          contains(github.event.pull_request.labels.*.name, 'tests:all-product')
        run: |
          # converts entire YAML file into JSON - no filtering since we want all PTs to run
          ./.github/bin/build-pt-matrix-from-impacted-connectors.py -v -m .github/test-pt-matrix.yaml -o matrix.json
      - name: Build PT matrix (impacted-features)
        if: |
          github.event_name == 'pull_request' &&
          steps.filter.outputs.product-tests == 'false' &&
          !contains(github.event.pull_request.labels.*.name, 'tests:all') &&
          !contains(github.event.pull_request.labels.*.name, 'product-tests:all')
        # all these envs are required to be set by some product test environments
        env:
          ABFS_CONTAINER: ""
          ABFS_ACCOUNT: ""
          ABFS_ACCESS_KEY: ""
          S3_BUCKET: ""
          AWS_REGION: ""
          TRINO_AWS_ACCESS_KEY_ID: ""
          TRINO_AWS_SECRET_ACCESS_KEY: ""
          DATABRICKS_AWS_REGION: ""
          DATABRICKS_133_JDBC_URL: ""
          DATABRICKS_143_JDBC_URL: ""
          DATABRICKS_154_JDBC_URL: ""
          DATABRICKS_164_JDBC_URL: ""
          DATABRICKS_173_JDBC_URL: ""
          DATABRICKS_UNITY_JDBC_URL: ""
          DATABRICKS_UNITY_CATALOG_NAME: ""
          DATABRICKS_UNITY_EXTERNAL_LOCATION: ""
          DATABRICKS_HOST: ""
          DATABRICKS_LOGIN: ""
          DATABRICKS_TOKEN: ""
          GCS_DATABRICKS_UNITY_JDBC_URL: ""
          GCS_DATABRICKS_UNITY_CATALOG_NAME: ""
          GCS_DATABRICKS_UNITY_EXTERNAL_LOCATION: ""
          GCS_DATABRICKS_HOST: ""
          GCS_DATABRICKS_LOGIN: ""
          GCS_DATABRICKS_TOKEN: ""
          DATABRICKS_UNITY_GCP_GCS_JSON_KEY: ""
          GCP_CREDENTIALS_KEY: ""
          GCP_STORAGE_BUCKET: ""
          SNOWFLAKE_ACCOUNT_NAME: ""
          SNOWFLAKE_URL: ""
          SNOWFLAKE_USER: ""
          SNOWFLAKE_PASSWORD: ""
          SNOWFLAKE_PRIVATE_KEY: ""
          SNOWFLAKE_PRIVATE_KEY_2: ""
          SNOWFLAKE_PRIVATE_KEY_2_PASSPHRASE: ""
          SNOWFLAKE_DATABASE: ""
          SNOWFLAKE_ROLE: ""
          SNOWFLAKE_WAREHOUSE: ""
          TESTCONTAINERS_NEVER_PULL: true
        run: |
          # converts filtered YAML file into JSON
          ./.github/bin/build-pt-matrix-from-impacted-connectors.py -v -m .github/test-pt-matrix.yaml -i impacted-features.log -o matrix.json
      - id: set-matrix
        run: |
          echo "Matrix: $(jq '.' matrix.json)"
          echo "matrix=$(cat matrix.json)" >> $GITHUB_OUTPUT
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "::error::Cancelling merge queue workflow because build-pt failed"
          gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  pt:
    runs-on: ${{ matrix.runners != null && matrix.runners || fromJSON('["self-hosted", "gha-fleet-ec2-4cpu-16gb-amd64", "latest" ]') }}
    # explicitly define the name to avoid adding the value of the `ignore exclusion if` matrix item
    name: pt (${{ matrix.config }}, ${{ matrix.suite }}, ${{ matrix.jdk }})
    if: always() && needs.build-pt.outputs.matrix != '{}'
    strategy:
      fail-fast: ${{ github.event_name == 'merge_group' }}
      matrix: ${{ fromJson(needs.build-pt.outputs.matrix) }}
    # PT Launcher's timeout defaults to 2h, add some margin
    timeout-minutes: 130
    needs: [ build-pt ]
    env:
      REPORT_TEST_SESSION_NAME: pt
      REPORT_SERVICE_NAME: ${{ matrix.suite }}-${{ matrix.config }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # checkout all commits, as the build result depends on `git describe` equivalent
          ref: |
            ${{ github.event_name == 'repository_dispatch' &&
                github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha &&
                format('refs/pull/{0}/head', github.event.client_payload.pull_request.number) || '' }}
      - uses: ./.github/actions/setup
        timeout-minutes: 15
        with:
          # The job doesn't build anything, so the ~/.m2/repository cache isn't useful
          cache: 'false'
          cleanup-node: ${{ format('{0}', matrix.suite == 'suite-compatibility') }}
      - name: Run ECR login
        uses: ./.github/actions/starburst-setup
        with:
          aws-access-key-id: ${{ vars.ENG_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENG_AWS_SECRET_ACCESS_KEY }}
          secret-id: ${{ vars.SEP_SECRETS_AWS_ID }}
          login-docker: true
      - name: Product tests artifact
        uses: actions/download-artifact@v8
        with:
          name: product tests and server tarball
      - name: Fix artifact permissions
        run: |
          find . -type f -name \*-executable.jar -exec chmod 0777 {} \;
      - name: Enable impact analysis
        if: |
          needs.build-pt.outputs.product-tests-changed == 'false' &&
          github.event_name == 'pull_request' &&
          !contains(github.event.pull_request.labels.*.name, 'tests:all') &&
          !contains(github.event.pull_request.labels.*.name, 'tests:all-product')
        run: echo "PTL_OPTS=--impacted-features impacted-features.log" >> $GITHUB_ENV
      - name: Product Tests
        id: tests
        env:
          ABFS_CONTAINER: ${{ vars.AZURE_ABFS_HIERARCHICAL_CONTAINER }}
          ABFS_ACCOUNT: ${{ vars.AZURE_ABFS_HIERARCHICAL_ACCOUNT }}
          ABFS_ACCESS_KEY: ${{ secrets.AZURE_ABFS_HIERARCHICAL_ACCESS_KEY }}
          AWS_ACCESS_KEY_ID: ${{ vars.TRINO_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TRINO_AWS_SECRET_ACCESS_KEY }}
          S3_BUCKET: ${{ vars.TRINO_S3_BUCKET }}
          AWS_REGION: ${{ vars.TRINO_AWS_REGION }}
          TRINO_AWS_ACCESS_KEY_ID: ${{ vars.GALAXY_TRINO_DATABRICKS_AWS_ACCESS_KEY_ID }}
          TRINO_AWS_SECRET_ACCESS_KEY: ${{ secrets.GALAXY_TRINO_DATABRICKS_AWS_SECRET_ACCESS_KEY }}
          DATABRICKS_AWS_REGION: us-east-1
          DATABRICKS_133_JDBC_URL: ${{ vars.DATABRICKS_133_JDBC_URL }}
          DATABRICKS_143_JDBC_URL: ${{ vars.DATABRICKS_143_JDBC_URL }}
          DATABRICKS_154_JDBC_URL: ${{ vars.DATABRICKS_154_JDBC_URL }}
          DATABRICKS_164_JDBC_URL: ${{ vars.DATABRICKS_164_JDBC_URL }}
          DATABRICKS_173_JDBC_URL: ${{ vars.DATABRICKS_173_JDBC_URL }}
          DATABRICKS_UNITY_JDBC_URL: ${{ vars.DATABRICKS_UNITY_JDBC_URL }}
          DATABRICKS_UNITY_CATALOG_NAME: ${{ vars.DATABRICKS_UNITY_CATALOG_NAME }}
          DATABRICKS_UNITY_EXTERNAL_LOCATION: ${{ vars.DATABRICKS_UNITY_EXTERNAL_LOCATION }}
          DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
          DATABRICKS_LOGIN: token
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          DATABRICKS_UNITY_GCP_GCS_JSON_KEY: ${{ secrets.DATABRICKS_UNITY_GCP_GCS_JSON_KEY }}
          GCS_DATABRICKS_UNITY_JDBC_URL: ${{ vars.GCS_DATABRICKS_UNITY_JDBC_URL }}
          GCS_DATABRICKS_UNITY_CATALOG_NAME: ${{ vars.GCS_DATABRICKS_UNITY_CATALOG_NAME }}
          GCS_DATABRICKS_UNITY_EXTERNAL_LOCATION: ${{ vars.GCS_DATABRICKS_UNITY_EXTERNAL_LOCATION }}
          GCS_DATABRICKS_HOST: ${{ vars.GCS_DATABRICKS_HOST }}
          GCS_DATABRICKS_LOGIN: token
          GCS_DATABRICKS_TOKEN: ${{ secrets.GCS_DATABRICKS_TOKEN }}
          SNOWFLAKE_ACCOUNT_NAME: ${{ vars.SNOWFLAKE_ACCOUNT_NAME }}
          GCP_CREDENTIALS_KEY: ${{ secrets.GCP_CREDENTIALS_KEY }}
          GCP_STORAGE_BUCKET: ${{ vars.GCP_STORAGE_BUCKET }}
          SNOWFLAKE_URL: ${{ vars.SNOWFLAKE_URL }}
          SNOWFLAKE_USER: ${{ vars.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_PRIVATE_KEY: ${{ secrets.SNOWFLAKE_PRIVATE_KEY }}
          SNOWFLAKE_PRIVATE_KEY_2: ${{ secrets.SNOWFLAKE_PRIVATE_KEY_2 }}
          SNOWFLAKE_PRIVATE_KEY_2_PASSPHRASE: ${{ secrets.SNOWFLAKE_PRIVATE_KEY_2_PASSPHRASE }}
          SNOWFLAKE_DATABASE: ${{ vars.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_ROLE: ${{ vars.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ vars.SNOWFLAKE_WAREHOUSE }}
        run: |
          MATRIX_SUITE="${{ matrix.suite }}"
          MATRIX_CONFIG="${{ matrix.config }}"
          EVENT_NAME="${{ github.event_name }}"
          EVENT_SCHEDULE="${{ github.event.schedule }}"
          PR_LABELS="${{ join(github.event.pull_request.labels.*.name, ' ') }}"
          SNOWFLAKE_LABEL_PRESENT=false
          if [[ " $PR_LABELS " == *" snowflake "* ]]; then
            SNOWFLAKE_LABEL_PRESENT=true
          fi

          if [[ "$MATRIX_SUITE" == "suite-starburst-snowflake" ]]; then
            if [[ "$EVENT_NAME" == "schedule" && "$EVENT_SCHEDULE" == "0 0 * * 6" ]] || \
               [[ "$SNOWFLAKE_LABEL_PRESENT" == "true" ]]; then
              echo "Running Snowflake PT weekly"
              testing/trino-product-tests-launcher/target/trino-product-tests-launcher-*-executable.jar suite run \
              --suite "$MATRIX_SUITE" \
              --config "config-$MATRIX_CONFIG" \
              ${PTL_OPTS:-} \
              --bind=off --logs-dir logs/ --timeout 2h
            else
              echo "Skipping Snowflake PT since it's not a scheduled build"
            fi
          else
            testing/trino-product-tests-launcher/target/trino-product-tests-launcher-*-executable.jar suite run \
            --suite "$MATRIX_SUITE" \
            --config "config-$MATRIX_CONFIG" \
            ${PTL_OPTS:-} \
            --bind=off --logs-dir logs/ --timeout 2h
          fi
      - name: Upload test results
        uses: ./.github/actions/process-test-results
        if: always()
        with:
          artifact-name: pt (${{ matrix.config }}, ${{ matrix.suite }}, ${{ matrix.jdk }})
          has-failed-tests: ${{ steps.tests.outcome == 'failure' }}
          upload-heap-dump: ${{ env.SECRETS_PRESENT == '' && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
      - name: Upload test results to Datadog
        uses: ./.github/actions/upload-test-results-datadog
        if: always()
        with:
          datadog-api-key: ${{ secrets.DATADOG_API_KEY }}
      - name: Update PR check
        uses: ./.github/actions/update-check
        if: >-
          failure() &&
          github.event_name == 'repository_dispatch' &&
          github.event.client_payload.slash_command.args.named.sha != '' &&
          github.event.client_payload.pull_request.head.sha == github.event.client_payload.slash_command.args.named.sha
        with:
          pull_request_number: ${{ github.event.client_payload.pull_request.number }}
          check_name: ${{ github.job }} with secrets
          conclusion: ${{ job.status }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - name: Should notify on failure
        id: should_notify
        if: always() && steps.tests.outcome == 'failure' && contains(matrix.suite, 'databricks')
        run: |
          # GITHUB_HEAD_REF is only populated for pull requests runs, for scheduled runs extract the name from GITHUB_REF
          branch_name="${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"
          echo "branch=$branch_name" >> "${GITHUB_OUTPUT}"
          if [[ $branch_name == "master" || $branch_name =~ ^release-.*-e$ ]]; then
            echo "result=true" >> "${GITHUB_OUTPUT}"
          else
            echo "result=false" >> "${GITHUB_OUTPUT}"
          fi
      - name: Slack notify failed tests to Connectors team
        if: always() && steps.tests.outcome == 'failure' && contains(matrix.suite, 'databricks') && steps.should_notify.outputs.result == 'true'
        uses: archive/github-actions-slack@433fef9978d3adae73168bfc9c5a7ce722780231 # v3.1.0
        with:
          slack-bot-user-oauth-access-token: ${{ secrets.SLACK_TOKEN }}
          slack-channel: C09HT9ETS73 # team-connectors-notify channel ID
          slack-text: |
            :boom: Databricks PT failed: ${{ matrix.suite }} on branch: ${{ steps.should_notify.outputs.branch }} (<https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|see CI job for details>) (:thumbsup: this message if you take care of this)
      - name: Launch Flake Tracker
        if: failure() && steps.tests.outcome == 'failure' && github.ref == 'refs/heads/master'
        continue-on-error: true
        uses: starburstdata/ai-flake-fixer/.github/actions/launch-ai-flake@main
        with:
          github-access-token: ${{ secrets.GITHUBACCESSTOKEN }}
      - name: Cancel merge queue workflow
        if: ${{ failure() && github.event_name == 'merge_group' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "::error::Cancelling merge queue workflow because pt (${{ matrix.config }}, ${{ matrix.suite }}, ${{ matrix.jdk }}) failed"
          gh run cancel "${GITHUB_RUN_ID}" --repo "${GITHUB_REPOSITORY}"

  build-success:
    if: ${{ always() }} # if `failure()` would not work for cancellations, `!success()` would not work for skipped jobs
    runs-on: [self-hosted, gha-fleet-ec2-2cpu-8gb-amd64, latest]
    needs:
      - artifact-checks
      - bigquery-tests
      - build-pt
      - build-test-matrix
      - check-commit
      - check-commits-dispatcher
      - error-prone-checks
      - google-sheets-tests
      - gpu-tests
      - hive-tests
      - iceberg-biglake-tests
      - maven-checks
      - path-filters
      - pt
      - redshift-cloud-tests
      - redshift-fte-tests
      - salesforce-tests
      - snowflake-nightly-tests
      - snowflake-weekly-tests
      - starburst-functions-ai-tests
      - synapse-prepare
      - synapse-stop-pool
      - synapse-tests
      - test
      - test-jdbc-compatibility
      - test-other-modules
    steps:
      - name: "Check results"
        run: |
          # generated by TestCiWorkflow
          echo '${{ needs.artifact-checks.result }}' | grep -xE 'success|skipped' || { echo 'Job "artifact-checks" failed' >&2; exit 1; }
          echo '${{ needs.bigquery-tests.result }}' | grep -xE 'success|skipped' || { echo 'Job "bigquery-tests" failed' >&2; exit 1; }
          echo '${{ needs.build-pt.result }}' | grep -xE 'success|skipped' || { echo 'Job "build-pt" failed' >&2; exit 1; }
          echo '${{ needs.build-test-matrix.result }}' | grep -xE 'success|skipped' || { echo 'Job "build-test-matrix" failed' >&2; exit 1; }
          echo '${{ needs.check-commit.result }}' | grep -xE 'success|skipped' || { echo 'Job "check-commit" failed' >&2; exit 1; }
          echo '${{ needs.check-commits-dispatcher.result }}' | grep -xE 'success|skipped' || { echo 'Job "check-commits-dispatcher" failed' >&2; exit 1; }
          echo '${{ needs.error-prone-checks.result }}' | grep -xE 'success|skipped' || { echo 'Job "error-prone-checks" failed' >&2; exit 1; }
          echo '${{ needs.google-sheets-tests.result }}' | grep -xE 'success|skipped' || { echo 'Job "google-sheets-tests" failed' >&2; exit 1; }
          echo '${{ needs.gpu-tests.result }}' | grep -xE 'success|skipped' || { echo 'Job "gpu-tests" failed' >&2; exit 1; }
          echo '${{ needs.hive-tests.result }}' | grep -xE 'success|skipped' || { echo 'Job "hive-tests" failed' >&2; exit 1; }
          echo '${{ needs.iceberg-biglake-tests.result }}' | grep -xE 'success|skipped' || { echo 'Job "iceberg-biglake-tests" failed' >&2; exit 1; }
          echo '${{ needs.maven-checks.result }}' | grep -xE 'success|skipped' || { echo 'Job "maven-checks" failed' >&2; exit 1; }
          echo '${{ needs.path-filters.result }}' | grep -xE 'success|skipped' || { echo 'Job "path-filters" failed' >&2; exit 1; }
          echo '${{ needs.pt.result }}' | grep -xE 'success|skipped' || { echo 'Job "pt" failed' >&2; exit 1; }
          echo '${{ needs.redshift-cloud-tests.result }}' | grep -xE 'success|skipped' || { echo 'Job "redshift-cloud-tests" failed' >&2; exit 1; }
          echo '${{ needs.redshift-fte-tests.result }}' | grep -xE 'success|skipped' || { echo 'Job "redshift-fte-tests" failed' >&2; exit 1; }
          echo '${{ needs.salesforce-tests.result }}' | grep -xE 'success|skipped' || { echo 'Job "salesforce-tests" failed' >&2; exit 1; }
          echo '${{ needs.snowflake-nightly-tests.result }}' | grep -xE 'success|skipped' || { echo 'Job "snowflake-nightly-tests" failed' >&2; exit 1; }
          echo '${{ needs.snowflake-weekly-tests.result }}' | grep -xE 'success|skipped' || { echo 'Job "snowflake-weekly-tests" failed' >&2; exit 1; }
          echo '${{ needs.starburst-functions-ai-tests.result }}' | grep -xE 'success|skipped' || { echo 'Job "starburst-functions-ai-tests" failed' >&2; exit 1; }
          echo '${{ needs.synapse-prepare.result }}' | grep -xE 'success|skipped' || { echo 'Job "synapse-prepare" failed' >&2; exit 1; }
          echo '${{ needs.synapse-stop-pool.result }}' | grep -xE 'success|skipped' || { echo 'Job "synapse-stop-pool" failed' >&2; exit 1; }
          echo '${{ needs.synapse-tests.result }}' | grep -xE 'success|skipped' || { echo 'Job "synapse-tests" failed' >&2; exit 1; }
          echo '${{ needs.test.result }}' | grep -xE 'success|skipped' || { echo 'Job "test" failed' >&2; exit 1; }
          echo '${{ needs.test-jdbc-compatibility.result }}' | grep -xE 'success|skipped' || { echo 'Job "test-jdbc-compatibility" failed' >&2; exit 1; }
          echo '${{ needs.test-other-modules.result }}' | grep -xE 'success|skipped' || { echo 'Job "test-other-modules" failed' >&2; exit 1; }
